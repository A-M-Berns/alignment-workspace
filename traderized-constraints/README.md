# Traderized Constraints

Lean companion to *Strengthening Logical Induction with Traderized Constraints*.
`TraderizedConstraints.Paper` is the paper-facing entry point; the proofs reuse the
implementation backend in `../lean/Workspace/Normativity/Contrib`.

Build with `lake build` from this directory.

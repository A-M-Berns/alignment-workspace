# Enforcement affordability

**Verdict: Proposition 6.1's divergence is an artifact of per-day worst-case
accounting for margined contested schedules, and real exactly where the
two-defect taxonomy says it is.** Liability is realized enforcement inventory
against budget-throttled opposing flow: at every verified fixed point the
realized enforcement coefficient tracks the realized opposing flow to within
the day's MarketMaker allowance, so available intensity never converts to
realized loss. Tolerance-independence survives; the K = {1/2} adjudication
goes to the margin account (homothetic core is sufficient-but-not-necessary,
a revenue lens mistaken for a solvency lens); the diachronic functional is
the set-gap path, not Hausdorff total variation, with compounding through
world-uniform extraction and settlement vindication; the exploitation
identity survives with at-horizon plausibility, support-local nesting, and
coalition closure. One dispatched sub-claim refuted: W3's linear
total-variation rate (adaptive pumps compound).

- `MEMO.md` — the prosecution, organized by the taxonomy, with the graded
  target table, G1 citations, and the paper-facing single-coordinate
  trichotomy sketch; then the paper-1 continuation (C0–C3): the one-coordinate
  affordability theorem with its four necessity witnesses, the self-financing
  lemma it needs, the criterion-forced impossibility, the Appendix D
  subsection, and the converse of Theorem 4.6.
- `THEOREM_MAP.md` — model ↔ skeleton ↔ Lean, with cited / strengthened /
  untouched lists, and the Lean-promotion table for the one-coordinate
  theorem.
- `src/market_model.py` — the one exact finite market model (rationals
  everywhere, MarketMaker guarantee verified on every run).
- `tests/` — 46 adversarial fixtures: the four tiers and the addendum's probes,
  plus the continuation's recycling, Appendix D and Theorem 4.6 families.
- `FOLLOWUP_STOCK.md` — material for the general characterization, collected
  and deliberately undeveloped.
- `PROVENANCE.md` — artifact and pinned-input provenance.

Run:

```sh
python3 tests/run.py
```

Nothing in this round is registered or kernel-checked. All names are
provisional.

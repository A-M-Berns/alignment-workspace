CONVERGENCE-IS-A-PROFILE-HULL-FALLS-UNDER-EVERY-NARROWING-DISCORD-IS-CERTIFIED-AND-REMOVED-ONLY-BY-DEFEAT-OR-REFUTATION-AND-HIDDEN-DISCORD-ESCAPES-THE-FRAGMENT — the convergence profile `(Hull, Ovl, Disc)` is defined on interval data over a declared fragment with declared positive weights and Lean-stated with its inequalities, of which the dispatch's `Disc = 0 → Ovl > 0` is false on touching intervals while `Disc = 0` is exactly "no declared coordinate in discord" under positive weights; fragment dilution, coordinate splitting, discord hiding and weight capture all succeed against a naive reading and are blocked by the unpinned normalization, pull-back through the transport rule, the product order on hull and discord, and declared weights respectively, while hidden discord — a merged bundle infeasible with every common interval nonempty, two reasoners or three with no pairwise incompatibility — is blocked by nothing and is certified only by the merged Farkas certificate; hull and overlap mass are nonincreasing and discord persists under every nesting, so settlement, shared narrowing and private narrowing lower the hull and never remove discord, private narrowing creates discord, and sharing the private warrant either leaves the discord or refutes the receiver, discord being removed only by defeat or by giving up a named item; hull mass converges under summable reopenings of the hull endpoints and oscillates without them; under exhaustive sharing read as equal live sets the discord set is eventually constant and every persistent discord is a certified incompatibility between substantive items, with the merged-bundle limit the dispatch stated corrected to one direction; discord is pairwise by Helly in dimension one and merged feasibility is not; hull mass for two reasoners is the sum of their unforced masses minus the overlap plus the discord gap; a lock is width or certified discord and stops no convergence; overlapping consensus — identical intervals on disjoint dockets — is exhibited and breaks under one one-sided defeater; the moral aggregation disagreement ends as discord with a certificate naming exactly the prioritarian and sum items; and Logical Induction's two-inductor guarantee, cited by declaration, is agreement on what settles and nothing on what does not, where this round's property lives.

Reading order: [`MEASURE.md`](MEASURE.md), [`GAMEABILITY.md`](GAMEABILITY.md), [`DYNAMICS.md`](DYNAMICS.md),
[`MANY.md`](MANY.md), [`CONSENSUS.md`](CONSENSUS.md), [`LI_COMPARISON.md`](LI_COMPARISON.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`FIXTURES.md`](FIXTURES.md), [`REPORT.md`](REPORT.md).
Lean: `lean/Workspace/Normativity/Contrib/ConvergenceMeasure.lean` (23 audited declarations,
`[propext, Classical.choice, Quot.sound]`), importing `SeedStatics.lean` unchanged.
Fixtures: `src/` (the seed round's `src/` loaded by path, unmodified), run by
`python3 tests/run.py` (20 tests).  Consumes `../2026-09-16-seed-statics/`.  Nothing is
registered.

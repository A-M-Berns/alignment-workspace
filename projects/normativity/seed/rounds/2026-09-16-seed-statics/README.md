FORCED-REGIONS-COMPUTE-ON-THE-EXISTING-COMPILER-CONFINEMENT-IS-A-HULL-BOUND-SEED-INDEPENDENCE-FAILS-UNDER-ITEM-HUMILITY-AND-HOLDS-UNDER-STRICT-HUMILITY — the seed is a typed object with three layers and two separate withdrawal channels, its forced region on a settled fragment is `Bundle.Region` of a computable bundle decided by the unchanged `checkCompiled`, its forced intervals are computed by the existing Fourier–Motzkin elimination with correctness and attainment proved and no LP optimum assumed, and intervals nest under settlement and widen under defeat; confinement holds as the width of the hull of the two forced intervals and is false as the length of their union, the common region being the merged seed's region; the landed defeat calculus is a trace discipline that fixes the live docket and conserves load but computes no extension, so under exhaustive exchange the live warrant set is a function of the trace and the grounded extension is a hypothesis one may impose, not a theorem; seed-independence on the point-forced fragment is refuted for humility read item-wise — two strengths of one half pin a coordinate — and proved when substantive endorsements are strict, since a point forced by a closed convex layer cut by an open layer is forced by the closed layer alone, so a humble seed narrows or breaks and never pins; strength selection is order-dependent and only reopening under a canonical joint rule removes it, which bears on the past-self-burn ruling and is filed; the forced intervals are eventually constant on a finite discovery class, oscillate forever on an infinite one, and converge when reopenings are summable; the moral fixture forces dominance at strength `2c − 1` and impartiality to at least one half, forces no additive aggregation, leaves interpersonal comparability as an open axis, and breaks on three population axioms with an exact Farkas certificate and a four-row minimal infeasible subset; the legal fixture instantiates the interface, separates amendment from defeat, and cannot express standing, court hierarchy, or a locked selection, which binding precedent needs; and the fused criterion is stated exactly and obstructed where the adapter cannot write prices and the enforcer's position is not quantified over, so limit behaviour is construction-relative.

Reading order: [`SEED.md`](SEED.md), [`STATICS.md`](STATICS.md), [`CONFINEMENT.md`](CONFINEMENT.md),
[`STABILIZATION.md`](STABILIZATION.md), [`CRITERION.md`](CRITERION.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`FIXTURES.md`](FIXTURES.md),
[`ALIGNMENT_READING.md`](ALIGNMENT_READING.md), [`REPORT.md`](REPORT.md).
Lean: `lean/Workspace/Normativity/Contrib/SeedStatics.lean` (42 audited declarations,
`[propext, Classical.choice, Quot.sound]`).  Fixtures: `src/`, run by
`python3 tests/run.py` (28 tests).  Consumes the rational-row compiler and the
Fourier–Motzkin elimination unchanged; cites the defeat landing.  Nothing is registered.

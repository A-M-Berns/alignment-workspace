# Report: anchored slices and authenticated semantic Transfer

## Result

Late-incurred content requires explicit anchored slices, at least up to an
equivalent append-only delta representation. A matter may acquire slices
without acquiring a second lifecycle: each slice has a birth, immutable
anchored denotation, and admission witness, while existing issue succession
carries its current matter-and-slice-indexed loads.

Generic Transfer cleanly separates:

- **accounting validity**, a finite join equation over supplied semantic values;
- **semantic authentication**, domain evidence connecting representations to
  the historically anchored slice meaning.

Accounting needs only a finite join-semilattice with bottom. Authentication is
necessarily domain-specific, though it has a generic proof-relevant interface
and soundness projection. Authenticated Transfer composes when the intermediate
interpretation is shared or connected by an authenticated join-preserving
commuting bridge into a stable slice domain.

Slice-wise Answerability Conservation follows by induction from authenticated
birth, local Transfer accounting/authentication, authenticated terminal exits,
and settled Continuity. Its No Semantic Laundering corollary forces weakening
to expose an authorized disposition and strengthening to expose a fresh origin.
No settled Continuity change is needed.

## Answers

1. **Explicit slice indexing?** Yes. A bare growing anchor erases the time and
   warrant of late accretion. A repaired delta anchor is slices under another name.
2. **New slices without a lifecycle?** Yes. Slices index semantic invariants;
   issues retain all succession, liveness, and closure mechanics.
3. **Translation versus accretion?** Translation preserves one anchored
   denotation under authenticated representation change. Accretion adds
   independently grounded content with a fresh birth and admission witness.
4. **Translation versus revision?** Equivalent change is translation.
   Non-equivalent change requires disposition of removed content and/or admission
   of a fresh increment.
5. **Required algebra?** Finite associative, commutative, idempotent join with
   bottom and its induced order.
6. **Is a join-semilattice enough?** Yes. No distributivity, complement,
   meet, atomic decomposition, or canonical subtraction is used.
7. **Generic accounting?** For each affected slice, incoming content equals
   authenticated satisfaction, authorized disposition, and the finite join of
   outgoing carrier loads.
8. **What is domain-authenticated?** Representation denotation, translations,
   admissions, dispositions, terminal witnesses, and any context/ontology bridge.
9. **Can a denotation map represent authentication?** It can state the
   semantic relation, but its provenance and soundness must be supplied by an
   anchored protocol, fixed model, or domain checker.
10. **How is it anchored?** Slice birth fixes a stable semantic domain and
    interpretation commitment; era-local meanings map into it through
    authenticated bridges.
11. **What blocks disguised weakening?** The old-slice equation. Any missing
    component must appear in an authorized Disposition term.
12. **Strengthening?** Transfer the old slice unchanged and admit the increment
    as a fresh slice.
13. **Does authenticated Transfer compose?** Yes, conditionally.
14. **Composition condition?** Same slice; validated local certificates; stable
    or transported context/target identifiers; and exact intermediate semantic
    agreement or a join-and-bottom-preserving authenticated commuting bridge.
15. **Time-varying ontology?** Yes when local meanings commute into the stable
    anchored slice domain. Arbitrary evaluator remapping is insufficient.
16. **Where does regress stop?** At an explicit fixed metalanguage/model
    relation or named checker soundness assumption. The calculus is
    parameterized by semantics; it does not manufacture meaning.
17. **Coverage schema?** Yes: authenticate target, applicability, scope,
    unresolved/registration status, and route-quality obligation.
18. **Reason schema?** Yes independently: authenticate provenance, content and
    target, unresolved status, and answer mode. Progress needs additional
    comparison structure.
19. **Continuity surgery?** None. Slices and authentication are strictly
    semantic indices and certificates over the settled lifecycle.
20. **Seed assumptions?** Slice-admission rules, sound domain checkers,
    future-ontology bridges, and protection against evaluator-controlled
    self-authentication.

## Dependency diagram

\`\`\`text
anchored admission + domain authentication       [semantic / seed assumptions]
                    |
                    v
per-slice finite Transfer accounting              [local semantic certificate]
                    |
                    v
Resolve + fresh successor sets + exact evolution  [settled Continuity]
                    |
                    v
matter ancestry + live carrier frontier           [settled structure + allocation]
                    |
                    v
Slice-wise Answerability Conservation             [finite-prefix induction]
                    |
                    v
No Semantic Laundering                            [semantic corollary]
\`\`\`

Progress/fairness are not used; they would be needed for eventual response.
Liability joint feasibility remains an independent constraint.

## Verification

\`python3 tests/run.py\` runs 21 tests: the twenty required hostile cases plus a
nondistributive/non-atomic algebra discriminator. These finite fixtures are
support for unregistered paper results, not registered checker verdicts.

## Naming and status

Provisional names: **anchored slice**, **Transfer accounting**, **semantic
authentication**, **Slice-wise Answerability Conservation**, and **No Semantic
Laundering**. All remain unregistered.

## Deviations

No Lean experiment was added: finite countermodels settle the interface and no
structural typing change is proposed. The round does not provide a morally
complete authentication oracle or solve seed quality; it names those
assumptions instead. The prompt record preserves every numbered instruction,
hostile case, deliverable, question class, and verdict option, but compresses
repeated display equations and framing prose rather than reproducing the
dispatch byte-for-byte.

## What is not shown

No eventual satisfaction, probabilistic Coverage, Progress comparison theorem,
liability bridge, or derivation of semantic correctness from syntax is shown.
The result is finite-prefix and relative to authenticated domain judgments.

## Outstanding maintainer actions

None.

\`ACCOUNTING-CLEAN-AUTHENTICATION-DOMAIN-SPECIFIC\`

\`AUTHENTICATED-TRANSFER-COMPOSES\`

### \`ANCHORED-SLICES-REQUIRED\`

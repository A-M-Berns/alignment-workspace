# Report

CONDITIONAL-SPINE-MECHANIZED — Export accounting, row-compiler soundness, declared-workload scheduling, affine transport, finite Progress, and LI preservation now compose in Lean; semantic bills, representability, and concrete connector certificates remain external or open.

The remaining program is not one gap. Six mechanical pieces now have kernel statements: structural obligation export from `DefeatTrace`; sound success/conflict results for a cited rational-row compiler; a bridge from a represented compiled schedule to the registered effective projection theorem; the declared finite predictable-window schedule; finite affine semantic transport; and the shared-service three-term Progress inequality. The final structure retains opaque certificates for Integrity, Robust Openness, settlement trust, export, compilation, and affordability, then concludes ordinary LI and the quadratic Progress bound.

The full history adapter is still incomplete. `export_sound` reads the landed issue/defeat trace and a supplied immutable `spec : Q -> Spec`. It proves exposure accounting, one structural fate, and carry. It does not authenticate `spec`, join settlement/service/compiler event types, or prove semantic identity along a disposition. Those are exact connector obligations.

The compiler result is intentionally restricted. A `compiled` result contains a checked rational point. A `conflict` result contains a sound Farkas certificate over cited and cube rows. `Unknown` is permitted. `failure_conserves` proves that every cited live obligation remains structurally carried whatever result is returned. Compiler completeness, semantic row realization, canonical coordinate/provenance checks, and an effective rows-to-vertices conversion remain open.

`CompiledDay.represents` is the representability hypothesis at the boundary actually consumed by the landed projection stack: equality between a `RationalPolytope` carrier and the real interpretation of the cited rational rows. Given that field and computation/admissibility premises, `compiled_end_to_end` is a direct specialization of `EffectiveRepresentation.end_to_end_of_constraints_effective`. Nonempty intersection is sufficient; no interior condition appears.

The scheduler theorem is for a declared finite linear workload. Each claim supplies a finite nonempty window and the schedule chooses a cheapest date in it. Theorems give the advertised liability sum, adaptedness, full row transport, zero residual over a service set containing all chosen dates, and total service equal to total claim. No market-enforcer connector or adversarial online theorem is inferred.

The practical-response theorem packages PR82's landed calibration, randomized argmax, and response composition into

```text
loss <= (2 L) d + (L (2 zeta + eta) + epsilon_resp).
```

It assumes authenticated ambiguity and response adequacy. It does not derive either from projection. Every positive transport edge in the final package carries its own inequality against its service's one realized defect; construction of a jointly compatible response remains open.

`progress_bound` is the general finite shared-service inequality. `progress_bound_quadratic` adds intensity normalization, `lambda_s d_s^2 <= rho_s`, and the finite weighted Jensen step to conclude

```text
Prog <= Gamma * sqrt((sum rho)/(sum lambda)) + epsilonBar + D * residual.
```

Thus the Progress arithmetic is no longer a paper-only gap. Producing the transport, edge certificates, amplification, and vanishing rates remains a research/realization task.

## Typed gap inventory

- Agent-sized and Lean-proved: trace accounting; structural export; failure conservation; rational witness and Farkas soundness; represented-schedule composition; declared-workload schedule; response algebra; n-step affine fold; uptake/Jensen algebra; finite Progress; conditional packaging with ordinary LI.
- Integration work: authenticated unified history record; typed `O_P` adapter from it; compiler provenance checker; effective rows-to-vertices conversion; per-date projection-work connector; market liability connector; construction of the finite package.
- Genuine research: useful convex representability classes; general online affordability; quantitative semantic-certificate generation; a concrete jointly compatible counterfactual-value/response ecology.
- Intentionally external: settlement integrity; counterfactual Non-Capture relative to declared interventions; causal/value identification and response adequacy.

## Integrity and Answerability

The implication depends on the bill. If `Integrity` includes authenticated lifecycle accounting, faithful successor carry, and settlement-backed terminal discharge, `export_sound` supplies its structural Diachronic Answerability consequence. Append-only record integrity alone does not. This round therefore keeps the predicates separate and records `Integrity -> DiachronicAnswerability` as a proposed theorem with an explicit strengthened premise, not a definition or an established general implication.

## Settlement and closure

Settlement trust authenticates monotone availability of a receipt through the external boundary. `Closes(H,s,alpha)` is an internal, history-relative judgment. Discharge needs the receipt, an internal closure certificate, and Integrity-authorized provenance. Later rejection of an earlier closure rationale should append a reconsideration obligation; no theorem or protocol for that event is supplied here.

## Deviations

The base is exactly the dispatched `5ff9539b44debc464128e6a1921ef1690f7b6487`.

`python3 -m checkers.workspace_state --json` reported a stale generated `state/views/NAMING_AUDIT.md` on the untouched base. This round did not edit the generated view because shared state is outside worker scope.

The final repository-wide `python3 tests/run.py` reaches that same stale-view defect through `checkers.wiki_state_bindings --self-test` and exits nonzero. The round-local exact tests, Lean module build, name lint, and dead-pointer gate pass. This is a base-state failure, not a failure introduced by the round.

The worktree contained an untracked 1,137-line Lean draft before this resumed execution. Its original generator was not recorded. The prompt requires truthful per-artifact generator provenance, so `PROVENANCE.md` records the original generator as unrecorded and separately identifies the executor who audited, repaired, and materially revised it. Treating the whole file as newly generated by the resumed executor would be false.

The commit carries the binding `Prompt-author-model:` trailer in addition to the prompt's requested `Model:` trailer. The common brief's “no other trailers” conflicts with `AGENTS.md`'s explicit requirement to record both when prompt author and executor differ; the binding repository rule governs.

## What this does not establish

- No unconditional legitimacy or Normative Inductor theorem.
- No proof that an arbitrary or concrete history satisfies Integrity or Robust Openness.
- No proof that Integrity in every plausible sense entails Answerability.
- No cryptographic, institutional, causal, or counterfactual settlement guarantee.
- No definition or theorem for revising a prior `Closes` judgment.
- No semantic soundness or completeness theorem for obligation-to-row compilation.
- No effective conversion from this round's row bundles to vertex polytopes.
- No general online or adversarial affordable scheduler.
- No theorem deriving action value from membership in a normative region.
- No construction of jointly compatible response certificates against one realized distribution.
- No generation theorem for quantitative semantic transport constants.
- No asymptotic theorem; the new Progress theorems are finite-horizon inequalities.

## Proposed filings

1. File a priority for the authenticated unified-history-to-`O_P` connector: consume this round's `ObligationExport` shape and prove anchor/provenance/settlement clauses.
2. File a priority for an effective row-schema compiler package: semantic row soundness, relative completeness, canonical identities, and rows-to-vertices representation.
3. File a priority for the concrete joint practical-response fixture: every transported edge certified against the one realized response distribution.
4. Record the stale `state/views/NAMING_AUDIT.md` base-state finding only if it persists after the orchestrator regenerates/checks shared state.

## New provisional names

`ObligationExport`, `Status`, `Fate`, `CarriedTo`, `Row`, `CitedRow`, `Bundle`, `FarkasCert`, `CompileResult`, `CompiledDay`, `CompiledSchedule`, `Workload`, `Step`, `Linked`, and `EndToEndHypotheses`.

## Outstanding maintainer actions

None. The proposed filings are recommendations for orchestrator synthesis, not decisions requiring maintainer-only information.

## Attribution

- Prompt author: orchestrator, Claude Fable 5.1 (Anthropic), relaying a maintainer dispatch.
- Executor: GPT-5 Codex (OpenAI).
- Date: 2026-09-05.
- Original uncommitted Lean draft generator: unrecorded.

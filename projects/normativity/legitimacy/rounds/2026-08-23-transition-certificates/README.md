# Reason-accounted transition certificates

**Verdict: the transition interface closes — three principles do the work of
the five proposed postulates, and Licensed becomes record-internally
substantive.** Part I repairs four defects in the PR #48 representation:
incompatibility was pairwise-decomposable in exactly the wrong direction and
becomes a set-level `Conflict` query; double negation was constructible and
becomes unconstructible; staged case views gain prefix-determined external
semantics that survive the delayed-evidence attack; and
applicability-in-source becomes enforced well-formedness over constitutive
instantiation declarations, with birth indices added. Part II derives a
certificate citing grounds, license, and lineage as separate sorts; every
self-certification and laundering attack dies on strict pre-state citation
plus immutability, with no dedicated non-laundering axiom. No-self-grounding
is a theorem, not a postulate.

- `MEMO.md` — prosecution, repairs, the final interfaces, postulate
  independence, and the Licensed verdict with its three named blockers.
- `src/reason_state.py` — repaired substrate.
- `src/transitions.py` — certificate, authority stand-in, checker.
- `tests/` — 43 adversarial checks covering the dispatch's fifteen required
  fixtures.
- `PROVENANCE.md` — artifact provenance.

Run:

```sh
python3 tests/run.py
```

Nothing in this round is registered or in Lean. All names are provisional.

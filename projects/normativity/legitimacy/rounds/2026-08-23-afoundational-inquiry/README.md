# Afoundational inquiry interface prosecution

**Verdict: partial unification under the tested representation, after temporal
and scheduling repairs.** The existing answerability kernel accepts inquiry
tasks as typed liabilities. The tested representation keeps due tokens,
coverage debts, and scheduling operationally distinct; whether they admit a
semantics-preserving compilation into a smaller event calculus remains open.
Authority ancestry is seed-terminating under strict pre-state checking; this is
a conservation result, not substantive justification. Event-time accrual is
coherent only with an ordered receipt/accrual/amendment semantics.

The restricted Set Cover with Delay and fixed-docket MLSC translations preserve
their objectives in the paper derivations. Independent finite implementations
of each side agree on nontrivial examples. Competitive service alone does not
imply nonstarvation under unbounded background load. Adaptive submodularity and
interactive submodular cover apply only to fixed finite uncertainty/service
models satisfying their respective hypotheses.

- `MEMO.md` states the repaired interface and its boundary.
- `THEOREM_MAP.md` records exact hypotheses, statuses, and counterexamples.
- `src/inquiry_kernel.py` contains exact finite witnesses.
- `tests/test_inquiry_kernel.py` runs 26 adversarial checks.
- `PROVENANCE.md` records artifact and source provenance.

Run:

```sh
python3 tests/run.py
```

Nothing in this round is registered or in Lean.
The initialization interface `S_0` does not settle normative bootstrapping
beneath the seed, and the `R -> O` compiler remains open.

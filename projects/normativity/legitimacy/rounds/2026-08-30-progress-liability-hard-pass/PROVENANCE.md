# Provenance

This round continues PR #69 at commit `f2d40c3` and treats its three earlier rounds as
upstream research state.

PR50 is audited without modification at open-PR head
`fa22b8a21cbd2bde81efe4cb0cd13d5551bbd51d`, including `MEMO.md`,
`THEOREM_MAP.md`, `FOLLOWUP_STOCK.md`, the exact market model, continuation fixtures,
and both prompt/report pairs. All PR50 positive grades remain explicitly
model-supported and unregistered.

Current theorem sources read directly:

- `ProjectionForce.lean`;
- `ProjectionBudget.lean`;
- `ProjectionCore.lean`;
- `ConstraintSchedule.lean`;
- `EnforcementPreservation.lean`;
- `AssessmentFirm.lean` and the relevant `AssessmentProcess.lean` Budgeter semantics.

New results are finite algebraic derivations over those displayed theorem interfaces
and exact-rational fixtures. No external literature was fetched or attributed. No
root registry, decision file, or PR50 artifact is changed.


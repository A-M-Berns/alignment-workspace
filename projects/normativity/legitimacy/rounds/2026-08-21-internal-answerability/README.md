# Internal-answerability kernel prosecution

**Verdict: adopt after four repairs, as a record-internal core only.** The
multiset equation needs input/output incidence and scoped closure proofs; semantic
transport must be per input; finite defeasible certificates need aggregate
invalidation keys; and joint trace semantics must precede an existential `Now`.

- `MEMO.md` — definitions, theorem/counterexample matrix, adversarial cases and
  recommendation.
- `src/kernel_witnesses.py` — finite counterexamples and set algebra.
- `tests/test_kernel_witnesses.py` — 17 checks, including the null-input case.

```sh
python3 tests/run.py
```

Nothing in this round is registered or in Lean.

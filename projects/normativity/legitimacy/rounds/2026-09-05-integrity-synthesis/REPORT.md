# Report

INTEGRITY-SEPARATES-RECORD-FAITHFULNESS-FROM-LIFECYCLE-CONSERVATION — the surviving program uses proof-relevant segment certificates, keeps Answerability's content-fate clause explicit, bills intervention-resistant openness and practical response externally, and leaves a finite list of typed NI connectors.

## Result

`SYNTHESIS.md` answers the thirteen dispatch questions. `DEPENDENCY_GRAPH.md` gives the
program dependency graph and `THEOREMS.md` preserves each evidence class.

The adversarial pass accepted the constructive lattice invariant but rejected its name
as sufficient Integrity. It found unchecked answer adequacy, self-grounded carry,
unprovenanced and mutable `Closes`, lost multiplicity, a bottom-ledger existential
witness, and untyped endpoint-witness compatibility. The synthesis therefore packages
record and lifecycle conditions and states the theorem conditionally.

The realization pass accepted the minimized Non-Capture bill. It accepted the direct
practical certificate only after identifying an exposure-loss/edge-loss mismatch. The
practical worker placed the resulting domination premise in the evaluation/transport
adapter and proved the residual transport inequality in Lean.

## Dependencies

This synthesis depends on:

1. `2026-09-05-integrity-constructive`;
2. `2026-09-05-integrity-adversary`;
3. `2026-09-05-noncapture-certificate`;
4. `2026-09-05-practical-certificate`;
5. `2026-09-05-ni-gap-audit`.

## Deviations

`origin/main` exactly matched the expected
`5ff9539b44debc464128e6a1921ef1690f7b6487`; the local `main` ref was stale at
`0ef93952a153509759f98793530e370a6e477083`, so every worktree used `origin/main`.

The five worktrees and untracked prompt/code drafts existed at intake. Each worker
audited rather than assumed those drafts. Their prompt files attribute the original
staging orchestrator to Claude Fable 5.1; this resumed orchestration and synthesis were
executed by GPT-5 Codex. The historical prompts were kept verbatim and the provenance
split is recorded rather than rewritten.

The constructive worker session terminated after its initial commit, so the requested
repair-after-B exchange could not be dispatched back to that session. B completed the
exact cross-review. This synthesis adopts B's concessions and restrictions; it does not
silently modify A's historical round.

All workers found the pre-existing generated view `state/views/NAMING_AUDIT.md` stale.
The synthesis refreshes generated state after registering the six rounds.

## What this does not establish

No canonical legitimacy predicate is registered. No application supplies the external
Non-Capture, settlement-trust, causal-value, or response-evaluator contracts. The
conditional NI theorem does not implement the concrete history/compiler/scheduler
connectors. The proposed Integrity package has not itself been formalized as one Lean
structure; its component theorems are formalized. The countermodels refute the tested
generic interfaces, not every possible definition called Integrity.

## Proposed later maintainer consolidation

Update the conceptual wiki only after review to separate record Integrity from the
obligation-specific lifecycle clause, replace typed-as-settling receipts by external
items plus internal stored `Closes`, place settlement suppression under the declared
Non-Capture bill, and state the direct practical certificate before its value-specific
factorization.

## Outstanding maintainer actions

None. The proposed conceptual consolidation is a recommendation, not a reserved
decision.

## Attribution

- Prompt author of the staged dispatch: Claude Fable 5.1 (Anthropic), relaying a maintainer dispatch.
- Resumed orchestrator and synthesis executor: GPT-5 Codex (OpenAI).
- Date: 2026-09-05.

# Dependency graph

```text
declared scope + intervention semantics                 settlement boundary
              |                                                  |
              v                                                  v
actual Coverage + NonCaptureCert --[Lean]--> RobustOpen     authenticated item s
                                                                |
history events --> RecordIntegrity                              + internal stored Closes
       |                 |                                      |
       +--> ContentLifecycle -----------------------------------+
                    |
                    v
             IntegritySegment --[Lean conditional]--> three-fate conservation
                    |                              \
                    v                               \ endpoint certificate
          qualitative export O_P                    v
                    |                         H_m <=leg H_n
                    v
 compiler + accountable Conflict/Unknown
                    |
                    v
 joint region + affordable schedule + bounded-liability LI preservation
                    |
                    v
 one response Pi_s + PracticalCert on every supported edge
                    |
                    v
 HeadlineToEdgeDominance + semantic transport + residual accounting
                    |
                    v
 finite Progress bound + ordinary Logical Induction
```

Solid arrows labelled Lean are proved in the worker modules. Unlabelled arrows require
an implementation or external certificate listed in `SYNTHESIS.md`.

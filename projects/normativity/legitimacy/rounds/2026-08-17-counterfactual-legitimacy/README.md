# Counterfactual legitimacy: does non-capture complete the architecture?

**Verdict: `architecture-positive / interface-splits-in-two /
projection-fixture-complete`.**

Internally accountable trajectory plus counterfactual non-capture is a viable
architecture for legitimate deference, and it is not one condition. Reason-
mediated non-capture rejects advisor-mediated versions of all six attacks that
survived the procedural round, while accepting autonomous bad revision, the
radical-transformation control, and persuasion by licensed reasons. It does not
reach an advisor that selects among genuinely licensed reasons, and cannot be
stated at all against an advisor that controls what arises. Both gaps close
under a second clause about **access** to due inputs, which needs an exogenous
supply of due reasons — a different object from the environment-relative target,
as the record-equivalence pair shows.

Two things the prosecution changed. The protected projection had to grow: attack
I installs a merge licence, which is neither one of the four coordinates nor a
field any record-internal condition reads. And a reason named by content and
coordinate is too coarse a trace — it must individuate what the reason licenses.

- `MODEL.md` — the paired-run model, the two channels, and what is provisional.
- `COUNTERFACTUAL_INTERFACE.md` — the conditions, quantifiers and coupling.
- `PROSECUTION.md` — the attack and control matrix.
- `THEOREM_MAP.md` — every statement, classified.
- `BOUNDARY.md` — non-capture against normative correctness, and who could check
  either.
- `src/`, `tests/`.

```sh
python3 tests/run.py
```

The procedural-legitimacy round's `src/` is a declared dependency: its
`Trajectory`, its four conditions and its target `L*` are the objects prosecuted
here rather than re-implementations of them.

Nothing here is registered in `CLAIMS.md` and nothing is in Lean.

# Reason-state narrow waist prosecution

**Verdict: the candidate waist survives after two structural additions and two
conventions, and the TMS framing is discarded rather than refined.** The
content language needs a constitutive contradiction floor and sources need a
receipt sort alongside claims; occurrences carry their staged applicability
claim among their sources, and applicability persistence across stages is an
ordinary defeasible schema, never a substrate default. The substrate itself
stores no labels, contexts, or statuses: it is five total stateless queries —
`Enabled`, `Reasons`, `Dependents`, `Explain`, `LostBasis` — and everything a
JTMS or ATMS does beyond that is one nameable stance policy plus caching,
with nogood caching shown unsound once incompatibility is learnable content.

- `MEMO.md` states the prosecuted interface, the reification table, the
  fifteen-example matrix, and the JTMS/ATMS/defaults comparison.
- `src/reason_state.py` is the finite reference model.
- `tests/test_reason_state.py` runs 37 adversarial checks.
- `PROVENANCE.md` records artifact and source provenance.

Run:

```sh
python3 tests/run.py
```

Nothing in this round is registered or in Lean. All names are provisional.

# Round-2 Work List — Consolidated, Disjoint TODOs

*Output of the CONSOLIDATION step. Five disjoint, sole-owned TODOs carved from the pooled
brainstorm candidates. Each executor and the faithfulness-skeptic are held to the spec below:
exact claim (acceptance target), off-limits adjacent results, modality, the SHADOW TEST
(what fake looks like vs. real), and the required non-vacuity witness.*

**Round-1 failure modes this carve is fixing.** (A) DUPLICATION — five threads re-proved v2 /
LeanDeference finite cores; here we DROP everything already established and merged the three Aumann
candidates / the two non-transitivity candidates / the three updateless candidates into one TODO each.
(B) FAKE LEAN — agents shipped finite δ=0 shadows with the hard LI/asymptotic/cross-agent content
smuggled in as a hypothesis. Every LEAN-CORE TODO below is a genuinely finite/decidable fact whose
TARGET object is the CONCLUSION (computed from the frame), never a hypothesis; the one EXEC TODO
searches the REAL DDB/LeanDeference inequality.

---

## GLOBAL OFF-LIMITS (already established — do NOT re-prove or re-skin in ANY TODO)

From `LeanDeference.lean` (confirmed sorry-free) and v2:

- `Deference.decomposition`, `value_of_defects`, `soft_nonneg`, `value_of_CM` — the finite Value
  skeleton (identity + CM ⇒ Value, defects = 0). Any pure `Finset` linearity re-derivation is a re-skin.
- `DeferenceAsymp.value_asymptotic` and the `Approx`/`AsympLE` calculus — Value modulo the five LI
  theorems as named hypotheses.
- `DeferenceExtra.softmax_lower_bound` (the `m_i − card·δ` softmax bound) and
  `DeferenceExtra.CM_implies_immodest` (CM-identity-at-fiber ⇒ immodest, ONE agent).
- `DeferenceArgmax.*` (argmax Value, payoff_gap_le_l1, softmax-limit), `DeferenceConverse.*`
  (`witness_identity`, `value_witness_iff_totalTrust(_mass)`, `totalTrust_of_value`, `value_iff_totalTrust`,
  the `AntiExpert` Fin-2 non-vacuity frame), `DeferenceFold.*`, `DeferenceConverseAsymp.*`,
  `DeferenceTrader.*` (round_profit ≥ gap).
- v2 §3 (Value), §5.2 (finite-collapse forces immodesty), §6 (Weatherson Coin/Bentham scope), §10
  (the §10 composition: LUV-Total-Trust ⇒ Value; "future self = maximal/join expert"; converse on the
  observable–bounded class). These are ESTABLISHED — re-stating them is duplication.

Round-1 lab Lean (OFF-LIMITS as objects — do not relabel/re-ship):
`Legitimacy.{defect_decomp, drug_defect_sign}`, `LegitimacyCorrigibility.{comply_iff_endorsed,
endorsed_signal_complies, adversarial_signal_resists, wirehead_declined}`,
`UpdatelessDeference.{split_eq_global, split_achieved}` and `UpdatelessDeference2.{defers_of_local_argmax,
endorsement_reduction}`, `MergingInductors.*` (selector monotonicity), `LateralDType.{WAR_of_argmax,
WIN_of_argmax, WAR_of_martingale}` (value_of_CM re-skins), `weak-endorsement*`, `UDT11Belief.*`.

**HYPOTHESIS-LAUNDERING BAN (applies to every TODO).** A TODO's TARGET object may NOT appear as a
hypothesis in that TODO's headline result. Specifically forbidden as hypotheses: the LI theorems
(ccee/cee/loe/expprovind/st), the cross-agent/LUV-Total-Trust premise, any `≂ₙ`/`≳ₙ` asymptotic
statement, "B is generated by its own market", and for each TODO the specific laundering listed in its
SHADOW TEST. "Compiles + sorry-free" is NOT evidence the claim is real.

---

## TODO 1 — Aumann's averaging step fails under modesty (overlapping-cell witness + partitional near-miss)

**id:** `aumann-modesty`  ·  **modality:** LEAN-CORE  ·  *(the flagship skipped result — CRITIQUE §8)*

**MERGES** three candidates that are one problem: "Finite Aumann-disagreement-under-modesty",
"Aumann-fails-under-modesty: a concrete 3-world frame", and "Aumann's averaging step fails under modesty
(the proof-step, not the slogan)" — and ABSORBS "Finite Aumann theorem proper (partitional ⇒ agreement)"
as the MANDATORY NEAR-MISS rather than a separate TODO. One brainstormer executed the naive nested
3-world "agree-to-disagree" version and found it FALSE/subtle; the surviving, genuinely-non-vacuous Lean
target is the **averaging-step** failure on a genuinely OVERLAPPING (non-partition) S4 cell cover.

**CLAIM (acceptance target).** Build an explicit finite common-prior frame: worlds `W` (3–4), a common
prior `π` (`∑π = 1`, all-positive or with explicit support), one agent whose information correspondence
`E : W → Set W` (equivalently `W → W → Bool`) is **reflexive + transitive but NON-partitional** (two
distinct cells `E(w)`, `E(w')` properly OVERLAP — neither nested nor disjoint), an event/RV `X : W → ℚ`,
and a self-evident (common-knowledge) event `C` that the cells COVER but do not PARTITION. Prove by
`decide`/`norm_num`:
  (i) the per-cell posterior expectation `E_π(X | E(w))` takes value `q1` on one covering cell and `q2 ≠ q1`
      on another covering cell of `C` (e.g. the brainstorm witness `π(A|cell@1)=1`, `π(A|cell@0)=1/2`,
      `π(A|C)=1/2`), so **no single common-knowledge value `q` exists**: Aumann's load-bearing averaging
      identity "if the posterior is common knowledge and equals `q` on the cells tiling `C`, then
      `π(X|C) = q`" FAILS, with the explicit witness;
  (ii) **NEAR-MISS (mandatory, must compile):** make the SAME prior's correspondence partitional
      (S5/Euclidean — replace the overlapping cover by the partition it refines to), and the averaging
      identity HOLDS: the per-cell posteriors that are common knowledge on `C` are forced EQUAL / their
      π-average equals the common value. This is finite-Aumann-proper on the concrete frame.

The lab-specific content: v2 §5.2's modesty/immodesty dichotomy **IS** the non-partition/partition
dichotomy — instantiated, not by analogy. Frame it as lab-specific (the finite reflection of "the AI
disagrees with me even after we share credences"), NOT as "we discovered Aumann fails non-partitionally"
(known — Geanakoplos; cite it, do not rediscover it).

**OFF-LIMITS adjacent.** `CM_implies_immodest` (one-agent, CM-identity ⇒ immodest fiber collapse) and
all of §5.2/§5.3 prose — do not re-derive immodesty-from-CM. The `AntiExpert` Fin-2 frame (one-agent
novice/expert Value frame) — different object, do not reuse as the "two-cell" witness. v2 §1.1's S4/S5/
Euclidean exposition is established; do not re-explain it as a result.

**SHADOW TEST.** FAKE: (a) the naive nested 3-world "agree-to-disagree" frame (one brainstormer SHOWED
this is false/subtle under nesting — shipping it is shipping a falsehood); (b) putting "agents disagree"
or "no single q" as a HYPOTHESIS and concluding a triviality; (c) defining common knowledge / the
self-evident event via a free Boolean label instead of the actual iterated-knowledge fixed point (meet /
`E`-closure), so "common knowledge" is unverified; (d) a frame whose cells do NOT genuinely overlap (then
it is partitional and there is nothing to witness). REAL: cells GENUINELY overlap (encode and `decide`
non-nestedness), `C` is `decide`-checked self-evident (closed under `E`, i.e. a fixed point of the
knowledge operator), the two distinct per-cell posteriors are computed from `π` and `E` (CONCLUSION, not
hypothesis), and the partitional near-miss compiles and forces agreement.

**NON-VACUITY WITNESS.** A concrete `π, E, X, C` with `decide`-checked: `C` self-evident & nonempty;
two covering cells with strictly different posteriors of `X` (`q1 ≠ q2`, exhibited as explicit rationals);
no common `q`. PLUS the compiled partitional near-miss on the same `π` forcing equality. `#print axioms`
clean for both. (Numeric witness to try first: brainstorm's `π(A|cell@1)=1, π(A|cell@0)=1/2, π(A|C)=1/2`.)

**WHY.** Cheapest real new result round 1 skipped; the most directly principal-relevant ("the AI
disagrees with me even after we share credences"); and it pins the lab's whole structural thesis
(modesty = non-partition) to a machine-checked object instead of prose.

---

## TODO 2 — Negative value-of-information: less-refined expert strictly higher Value (Weatherson tightness)

**id:** `negative-voi`  ·  **modality:** LEAN-CORE

**CLAIM (acceptance target).** Build the explicit finite prior frame (Weatherson's 3-world tightness
example, cited-but-unbuilt in v2 §1.1 line 241) with two experts `E1, E2`, both reflexive-transitive-
nested (S4), `E2` NON-partitional, and a menu `O` of `[0,1]`-options. With the recommended strategy for
each expert defined as the genuine **argmax-of-posterior** (`S_E(w) := O^{argmax_j E_π(O^j | E(w))}`,
ties broken by an explicit rule), prove by `norm_num`:
  (i) `Value(E1) < Value(E2)` STRICTLY — the recommended-strategy expected return under the MORE-refined
      `E1` is strictly LESS than under the coarser non-partitional `E2`: value of information is NEGATIVE;
  (ii) **NEAR-MISS (mandatory, must compile):** refine `E2` to its partitional coarsening (or refine to
      partitional anchor) and the SAME `O` gives `Value(E1) ≥ Value(E2)` — Blackwell/Geanakoplos
      monotonicity restored. This certifies the negative sign is caused by non-partitionality, not by the
      payoffs.

This is the witness that "the future self dominates only because it is the maximal/partitional
refinement" (v2 §10(a)) genuinely NEEDS the partitional anchor — the tightness of Geanakoplos VoI≥0.

**OFF-LIMITS adjacent.** v2 §1.1 STATES Geanakoplos needs "E2 partitional" and CITES the 3-world example
in prose only (no frame) — building the frame is the new content; do not re-state the prose. Geanakoplos
VoI≥0 is established prose. `value_of_CM`/`value_of_argmax`/`payoff_gap_le_l1` (the positive Value route) —
do not re-skin. The `AntiExpert` frame (one expert vs unconditional martingale, Total-Trust failure) is a
DIFFERENT phenomenon — do not reuse.

**SHADOW TEST.** FAKE: (a) choosing payoffs so the inequality holds for a reason unrelated to information
(e.g. defining `E1`'s "recommended strategy" adversarially / not as argmax-of-its-own-posterior); (b)
assuming "E2 better" as a hypothesis; (c) shipping only the strict inequality with NO near-miss (then it
looks possibly-vacuous / could be an arithmetic artifact). REAL: BOTH experts use
recommended = argmax-of-posterior computed from `π` and `E_k`; the strict gap is the CONCLUSION; and the
partitional near-miss compiles and flips the inequality back to `≥`.

**NON-VACUITY WITNESS.** Concrete `π, E1, E2, O` with `norm_num`-checked `Value(E1) < Value(E2)` as
explicit rationals, both correspondences `decide`-checked reflexive+transitive+nested with `E2`
non-partitional, plus the compiled partitional near-miss giving `Value(E1) ≥ Value(E2)`. `#print axioms`
clean.

**WHY.** Negative value-of-information is the sharpest finite face of "a less-capable expert can be a
better one to defer to," and it isolates *which* structural property (partitionality) the deference-to-
future-self dominance silently relies on. Directly bears on when delegating to a more-powerful system is
NOT automatically better.

---

## TODO 3 — Trust-laundering: Total Trust / endorsement is NOT transitive (EXEC search over the real inequality)

**id:** `trust-laundering`  ·  **modality:** EXEC

**MERGES** "Legitimacy is non-transitive: finite 3-agent witness" and "Trust-laundering: Total Trust is
not transitive (EXEC over the real inequality)" — one problem (delegation/chain non-closure). Routed EXEC
because the faithful object is a SEARCH over the actual `DeferenceConverse.value_witness_iff_totalTrust_mass`
conditional-mass inequality across a decidable test family — a hand-coded three-distribution Lean check
dressed as "trust is not transitive" (with the search done off-stage) would be a shadow.

**CLAIM (acceptance target).** Over an executable family of finite frames (Python/sympy; small `W`,
rational priors `π_H, π_A, π_B`, rational thresholds `t`, test variables `X` over a decidable grid),
EXHAUSTIVELY or randomly search for a triple where, using the EXACT DDB/LeanDeference Total-Trust
inequality `s·π{E(X)≥s} ≤ E_π(X·1[E(X)≥s])` (the one `value_witness_iff_totalTrust_mass` certifies):
  - `π_H` Total-Trusts `P_A` (holds for all tested `X, t`),
  - `π_A` Total-Trusts `P_B` (holds for all tested `X, t`),
  - yet `π_H` does NOT Total-Trust `P_B` (FAILS at some explicit `X, t`).
Report the explicit witness (brainstorm already located one: `H→B` fails at `X = 1[world 2], t = 1/3`).
POSITIVE CONTRAST (required): characterize empirically WHEN transitivity is recovered — the conjecture is
that it holds exactly when `B` is `H`-observable (shared weight-class / nestedness), which is v2 §10.3(a)
read from the delegation side. Show on the search that imposing the recovery condition makes the long edge
hold across the family.

Safety reading (state plainly, do not overclaim): **alignment is not closed under delegation** — "an AI
vetted by another AI" is not automatically trustworthy. Scope honestly: a decidable finite-family finding,
not a theorem about all inductors; the recovery-characterization in the LI cross-agent setting (D3) is
PROOF-ONLY and explicitly out of scope here.

**OFF-LIMITS adjacent.** `DeferenceConverse.*` proves `Value ⟺ Total Trust` for ONE fixed two-party pair
(incl. `AntiExpert` non-vacuity) — entirely two-party; do not re-prove it. `Legitimacy.*` /
`LegitimacyCorrigibility.*` (one-pair defect/comply objects) — do not relabel. v2 §10.3(a)/§10.4
(future-self-as-join) is established prose. The EDT-conditional reflection definition (`endorses`) must be
the genuine conditional-reflection form, not "equal means".

**SHADOW TEST.** FAKE: (a) proving non-transitivity by ASSUMING the weight-class mismatch / observability
failure that causes it (laundering the obstruction into a hypothesis); (b) a Lean file hard-coding three
distributions checking one inequality fails, dressed as "trust is not transitive", with the actual search
done off-stage (gives no evidence it is not a hand-picked artifact); (c) picking credences where one link
(`endorses H A` or `endorses A B`) actually FAILS — a vacuous chain; (d) defining endorsement /
Total-Trust non-faithfully (e.g. "equal means", or an inequality that is not the conditional-mass form).
REAL: an executable search over a genuine decidable test family computing the ACTUAL conditional-mass
inequality; BOTH short links verified to HOLD across all tested `(X,t)`; the long edge exhibited to FAIL
with an explicit `(X,t)`; plus the recovery-condition contrast run.

**NON-VACUITY WITNESS.** The explicit `(π_H, π_A, π_B, X, t)` triple printed by the search with: both
links Total-Trust-holding (verified over the full tested `(X,t)` grid, not just one point), the long edge
Total-Trust-failing at a printed `(X, t)` with the numeric gap. PLUS the recovery-condition sweep showing
H-observability/nestedness restores the long edge across the family. Search must cover frames where the
weight classes genuinely differ (else the swap is trivially legal and the search proves nothing).

**WHY.** Delegated/laundered trust is a concrete multi-agent-AI safety concern ("can you trust an AI
vetted by another AI?") and round 1 left D12 non-transitivity as a single undeveloped table row. An
EXEC witness over the real inequality is honest validation a Lean shadow cannot give.

---

## TODO 4 — Averaging hides spikes: asymptotic-average Value gives NO single-round safety bound

**id:** `averaging-hides-spikes`  ·  **modality:** LEAN-CORE

**CLAIM (acceptance target).** Make CRITIQUE §2b precise. Two compiled pieces:
  (i) **The existence direction (the real content):** exhibit an explicit finite family `a : Fin T → ℝ`
      (parameterized by horizon `T` and budget `B`) — the per-round adverse swings — with a DESIGNATED
      round `k` whose swing `a k = B` (the full trader budget, a CONSTANT independent of `T`), such that
      the T-round AVERAGE `(∑ a_i)/T = B/T → 0` as `T → ∞` (Value "holds" on average) while the SUP
      `max_i a_i = B` does NOT → 0. Prove BOTH limits: the average `→ 0` (e.g. `(B/T : ℝ) → 0` via
      `tendsto`), and the single-round swing stays `= B`. So **average-smallness yields no single-round
      bound**: a bounded total budget permits one round to absorb the whole budget `B` while contributing
      only `O(B/T)` to the average.
  (ii) **NEAR-MISS (mandatory, must compile):** if the per-round budget is forced to be `o(1)` (each
      `a_i ≤ c_i` with `c_i → 0`, i.e. NO single-round budget concentration), then `sup_i a_i → 0` too —
      certifying the vulnerability is EXACTLY unbounded single-round budget concentration.

**OFF-LIMITS adjacent.** `DeferenceTrader.round_profit_ge_gap` / `gap_pos_imp_profit_pos` prove the
per-round LOWER bound favoring the truster — do not re-skin; this TODO is the dual UPPER-bound *failure*.
`DeferenceAsymp`/`value_asymptotic` and the `Approx`/`AsympLE` calculus — do not import the LI asymptotic
Value content; this is a standalone finite/limit arithmetic fact about averages vs. sups.

**SHADOW TEST.** FAKE: (a) proving `(∑ a_i)/T ≤ max_i a_i` (TRUE, TRIVIAL, and the WRONG DIRECTION) and
dressing it as the vulnerability — this is the pre-registered fake; (b) a family where the average does
NOT actually → 0, or where the spike is not a fixed constant `B` independent of `T` (then "avg→0 while
sup↛0" is not exhibited); (c) shipping only the existence direction with no `o(1)`-budget near-miss (then
the claim "the cause is single-round concentration" is unsubstantiated). REAL: the EXISTENCE direction —
a concrete family with avg `→ 0` AND one coordinate `= B` constant, with BOTH limits PROVED (the
`Tendsto` for the average, the equality for the spike) — plus the compiled `o(1)`-budget near-miss giving
`sup → 0`.

**NON-VACUITY WITNESS.** The explicit family (e.g. `a i = if i = k then B else 0`) with compiled
`Tendsto (fun T => (∑_{i<T} a_i)/T) atTop (𝓝 0)` and `(fun T => sSup/max) = B` (not → 0); plus the
compiled near-miss `(∀ i, a_i ≤ c_i) → c_i → 0 → Tendsto (max) atTop (𝓝 0)`. `#print axioms` clean.

**WHY.** The single most important scope-limit for a safety principal: the LI deference framework is
structurally asymptotic-average and SILENT on individual high-stakes rounds — a treacherous trader can
dump its whole budget into one decision. CRITIQUE §2b says this belongs in the executive summary, not as
open-problem #4. A compiled avg-vs-sup separation makes the limit precise and undeniable.

---

## TODO 5 — EDT-derived node-value: kill the argmax tautology in updateless deference

**id:** `edt-node-value`  ·  **modality:** LEAN-CORE

**MERGES** "EDT-derived node-value: kill the argmax tautology" (the LEAN-CORE core) and ABSORBS the
acausal-coupling divergence ("Acausal coupling exhibits genuine UDT/EDT divergence") as the MANDATORY
near-miss. NOTE: this TODO does NOT take on the Geometric-UDT VoI-sign-flip candidate (dropped — see
below); it is solely about replacing the round-1 free `node_value` by a genuine EDT-conditional.

**CLAIM (acceptance target).** Replace the round-1 FREE input `node_value` by the genuine EDT-conditional
of the SAME utility: `v_s(a) := E_p[U(π) | reach s, π(s)=a]` under an EXPLICIT self-prediction/reachability
kernel `κ`. Prove (LEAN-CORE, finite) that in the **DECOUPLED** case (no cross-situation dependence: `U`
separable AND `κ` makes `reach s` independent of actions elsewhere) the EDT-derived per-node argmax
coincides with the global UDT optimum — and state this as a genuine relation whose ONLY inputs are `U`
and `κ` (so `node_value` is DERIVED, not hand-fed). The acceptance bar is that the derivation genuinely
involves `κ`:
  (i) decoupled coincidence: the policy that picks the EDT-derived argmax at each `s` attains the global
      `policyValue` optimum, with `v_s` defined via `κ` (the conditional), not as the local argmax of `U`;
  (ii) **NEAR-MISS (mandatory, must compile):** a COUPLED instance (cross-situation `κ` or non-separable
      `U`) where the EDT-conditional argmax PROVABLY DIVERGES from the global UDT optimum, the gap being
      exactly the acausal payoff. This certifies `v_s` is not a relabel of the local argmax.

**OFF-LIMITS adjacent.** `UpdatelessDeference.{split_eq_global, split_achieved}` and
`UpdatelessDeference2.{defers_of_local_argmax, endorsement_reduction}` — "argmax of a separable U is
globally optimal" with a FREE `node_value`. Re-introducing ANY free per-node valuation, or re-proving
`split_eq_global` under a new name, is the round-1 duplication and is FORBIDDEN. `LateralDType.*` /
`value_of_CM` re-skins — off-limits.

**SHADOW TEST.** FAKE: (a) re-introducing a free per-node valuation (the off-limits round-1 object); (b)
re-proving `split_eq_global`/`split_achieved` under a new name; (c) defining the EDT-conditional `v_s(a)`
so that it DEFINITIONALLY equals the local argmax of `U` (then the "derivation" is a relabel and the
kernel `κ` is inert — check `κ` actually appears load-bearingly); (d) shipping only the decoupled case
with no coupled near-miss (then the conditional could be a vacuous relabel). REAL: `v_s` is computed as a
genuine conditional through `κ` (the reachability/self-prediction kernel), it is NON-trivially equal to
the local argmax ONLY in the decoupled case, and the coupled near-miss compiles exhibiting a strict
divergence equal to the acausal payoff.

**NON-VACUITY WITNESS.** A concrete decoupled instance (`S, A, U, p, κ`) where the EDT-derived argmax
policy attains the global optimum (`norm_num`/`decide`), with `κ` appearing in the definition of `v_s`
(verify it is not eliminable); PLUS a concrete coupled instance (a counterfactual-mugging or Twin-PD-style
`κ`) where the EDT-conditional argmax `≠` the global UDT optimum, the difference computed and equal to the
acausal payoff. `#print axioms` clean. ("Mugging-caver fails to defer" must become a theorem about `U`
and `κ`, not a hand-fed verdict.)

**WHY.** Round 1's updateless relation was a tautology wrapper around argmax with a hand-fed
discriminator (CRITIQUE §3, BLOCKER): it could not tell a caver from a non-caver and accepted garbage
updates. Deriving `node_value` as the EDT-conditional is the red-team's named fix and the only honest
way to make updateless deference a result rather than a relabel. Bears on whether an agent's deference to
its updated self is principled or stipulated.

---

## DROPPED candidates (with reason)

Not TODOs — either established, only shadow-validatable, or duplicative of the five above.

- **Geometric-UDT VoI sign-flip (value/factual asymmetry).** High overlap with TODO 1 (VoI sign on
  non-partitional abstractions) and TODO 5 (the updateless layer); routing it separately risks two
  executors producing overlapping VoI-sign Lean. The honest finite core it offers (a non-partitional
  abstraction with VoI<0) is SUBSUMED by TODO 2's negative-VoI frame. Dropped to keep TODOs disjoint.
- **Vingean / intentional-stance VoI≥0 lemma + transitivity-load-bearing near-miss.** The positive
  direction is a `value_of_CM`/Geanakoplos re-skin risk; its genuinely-new part (non-transitive
  experiment with VoI<0) overlaps TODO 2's near-miss structure. Marked "hard" by the brainstormers and
  at high re-skin risk. Dropped.
- **Generable-feature closure (B-observability as a legal H-feature).** Adjacent to §10 typing; the real
  lemma risks either vacuity (define "generable" so broadly anything qualifies) or laundering (assume
  LUV-Total-Trust). Brainstormers flagged both failure modes; not a clean disjoint win. Dropped.
- **Strict corrigibility under positive endorsed mass.** A strict refinement of an OFF-LIMITS weak
  result (`endorsed_signal_complies`); thin, low trust-bearing, and adjacent enough to the off-limits
  object to risk a re-ship. Dropped (easy but not worth a summer step).
- **Non-circular legitimacy (truth-tracking ⇒ defect sign).** Genuine, but its honest validatable core
  overlaps TODO 3 (the cross-martingale defect / endorsement faithfulness) and TODO 5 (deriving a sign
  from a process rule rather than hypothesizing the overshoot). Folding its spirit into those keeps the
  list disjoint; as a standalone it risks re-circularizing or re-importing the off-limits overshoot
  hypothesis. Dropped.
- **Two trust knobs (δ, f) margin trade-off.** High risk of collapsing to `softmax_lower_bound` with a
  renamed constant (brainstormer's own instruction: "if it collapses to the existing lemma, DROP it").
  Dropped pre-emptively.
- **B-is-not-an-inductor: EXEC exploit-search AND PROOF-ONLY negative.** The executable mock-LI market
  risks being a caricature proving nothing ("hard"); the PROOF-ONLY version is an honest "no Lean here"
  but is two candidates for one negative, and the EXEC budget is better spent on TODO 3 (a search over a
  REAL established inequality, not a hand-built market). Dropped.
- **§10 faithfulness search (premise-holds-but-Value-fails).** Hard to guarantee the premise and
  conclusion are computed by INDEPENDENT routes (else circular); the expected outcome is "no
  counterexample", which is weak validation. Dropped in favor of TODO 3's sharper non-transitivity search.
- **Christiano reflective-probability (PROOF-ONLY) and Dependent-typed why-ain'cha-rich (PROOF-ONLY).**
  Both correctly self-identify as NOT Lean-tractable (infinitary fixed point / truth-definability
  obstruction). Honest, but round 2 is prioritizing the LEAN-CORE/EXEC items that yield REAL validation;
  these are deferred, not faked.
- **Hop-2 two-weighting-class substitution search (EXEC).** Speculative; the conjecture
  ("YES iff 𝒢_H ⊆ 𝒢_A after the shift") is the round-1 "one true gap" and the search risks only frames
  where the classes coincide. Higher-value EXEC budget went to TODO 3 (a search over an established
  inequality with a witness already in hand). Deferred.

# Round-3 Scout — Prior-Round Handoff Lens

*Scout deliverable, 2026-07-01. Lens: institutional memory — what rounds 1–2 deferred, dropped,
or were told to do next, and the highest-value follow-ups that honor the GLOBAL OFF-LIMITS list.
Sources read in full or in the load-bearing parts: run2/todos/TODOS.md (incl. DROPPED),
run2/report/{RESEARCH-REPORT,CRITIQUE}.md, all five run2/verify/*-verdict.md, round-1
report/{RESEARCH-REPORT,CRITIQUE}.md, audit/lean-verify-report.md; anchored against
li-deference.md §0.3, deference-in-logical-induction-v6.md §§4–8, faithful-acceleration.md §§5–8.
Every claim below is labeled: [ledger] = read directly from a prior-round file; [interpretation] =
my synthesis.*

---

## A. What rounds 1–2 explicitly deferred or dropped, WITH reasons [ledger]

### A.1 Deferred with an explicit "do this next" (run2 report §6, in priority order)
1. **`trust-laundering` faithful re-run** — the run's one cross-agent object was verdict **SHADOW**
   (32-point {0,1}-indicator grid substituted for the `∀X:W→ℝ` quantifier; the printed witness is a
   vacuous chain — both short links fail under genuine Total Trust). run2 report §6.1 names the fix:
   the conditional-mass inequality is **linear in X**, so on a fixed finite frame the property is an
   exact finite LP / vertex check, not a sampled grid. "Until then cite none of the numbers." The
   gate located 6 honest witnesses (one fine-grid-verified: π_H=(1/4,1/2,1/4), π_A=(1/4,1/4,1/2))
   but these are gate-located, not author-validated, and still grid- not exactly-verified.
2. **`aumann-modesty` proper self-evident sub-event** — REAL but `C` = whole space makes the
   self-evidence checks "true but trivial in any S4 frame" (critique §1b); rebuild with a proper
   S4 fixed point; also kernel-confirm attack2 (never reached a kernel exit).
3. **`negative-voi` strict near-miss** — the mandatory near-miss was an equality 4/9 = 4/9 (tie, not
   flip); find a menu where the partitional anchor makes the refined expert *strictly* win.
4. **`averaging-hides-spikes` true cause-isolation** — the spec near-miss was proved FALSE
   (a single early spike pins the running sup forever); the shipped substitute binds only the
   per-round swing and the vulnerable family satisfies it. "Closing the 1.5-of-2 gap" is open.
5. **`edt-node-value` κ-selection bridge** — REAL and the run's most substantive new Lean, but
   "the open problem moved from `node_value` to κ, it was not closed"; report §6.5 asks for the
   condition on κ under which the EDT/UDT divergence vanishes (PROOF-ONLY first).

### A.2 Dropped at consolidation, with reason (run2 TODOS §DROPPED)
- **Christiano reflective-probability** and **dependent-typed why-ain'cha-rich** — both PROOF-ONLY
  (infinitary fixed point / truth-definability obstruction); "deferred, not faked."
- **Hop-2 two-weighting-class substitution search (EXEC)** — the round-1 "one true gap"
  (ℙ^A→ℙ^H standpoint shift); dropped because the search risks only frames where the classes
  coincide, and the EXEC budget went to trust-laundering. Still open in v6 §8 (D3 bullet:
  "discharged in zero cases").
- **B-is-not-an-inductor** — mock-LI market risks caricature proving nothing.
- **Geometric-UDT VoI sign-flip** — subsumed by negative-voi + edt-node-value.
- **Vingean VoI≥0 + non-transitive near-miss** — re-skin risk of value_of_CM/Geanakoplos.
- **Generable-feature closure** — vacuity or laundering both flagged.
- **Strict corrigibility under positive endorsed mass** — thin refinement of an off-limits object.
- **Non-circular legitimacy (truth-tracking ⇒ defect sign)** — risks re-circularizing or importing
  the off-limits overshoot hypothesis; spirit folded into TODO 3/5.
- **Two trust knobs (δ,f)** — collapses to `softmax_lower_bound` with a renamed constant.
- **§10 faithfulness search** — expected outcome "no counterexample" is weak validation.

### A.3 Named open items that survived BOTH rounds untouched
- **Soft⇒hard spectral-gap step of v2 §5.2 / v6 §2.2** — round-1 report §4 calls it "the single
  highest-value future Lean target (Q3)"; round-1 critique §10.2: the finite-collapse impossibility
  "is NOT machine-checked anywhere; only its trivial tail is" (`CM_implies_immodest`). Not in run2's
  TODO carve at all. Still listed open in v6 §8.
- **Local (question-relative) deference / DDB's local conjecture** — round-1 phase-1 item 2
  ("near-trivial, settles a DDB open conjecture, Lean-ready"), never executed; still in v6 §8.
- **The manipulation theorem's four ingredients** (v6 §4.7/§8): second calibration condition
  separating calibration-to-self from calibration-to-truth; evidence/preemption; transfer-of-trust;
  **non-recoverability** ("legitimacy cannot be certified from the trace" — asserted, never proved).
- **Faithful acceleration's admissible domain** (v6 §8 bullet 1, "the deepest open item of the
  positive result"): no syntactic fragment works (Gödel rebuilds the quote diagonal); the
  "both faces die on the diagonal" boundary claim is §5.10 **prose**, not kernel-checked; and
  faithful-acceleration.md §8 obligation 3 ("no hidden hard settlement") is "now the deepest
  residual."
- **Minimal market model in Lean** (v6 §8, from lean-deference AUDIT): the cross-process *forcing*
  is named, not proved — T1/T3 are "squeezes over hypotheses that are the conclusion restated."
- **Legitimacy formalization itself** (li-deference §0.3, v6 §6.5/§8): "replacing 'all futures'
  with 'non-corrupt futures' in the target … currently a desideratum, not a model."

## B. What the critiques said the next round MUST do [ledger]

- run2 CRITIQUE §0 + report §0 (the boundary sentence, in both rounds): **"No cross-agent or
  asymptotic trust statement is machine-checked anywhere; the four REAL results bear on trust by
  INTERPRETATION, not derivation."** Round 2's one cross-agent artifact was the SHADOW. Crossing
  this boundary honestly — even for one concrete finite frame — is the standing dare.
- run2 CRITIQUE §5: trust-laundering's work writeup "still states the falsehood as fact"; nothing
  from it may be cited before a faithful re-run.
- run1 CRITIQUE §1: the merge is hand-waved at exactly B-inductor-ness, Hop 2, B-observability;
  "SALVAGEABLE" must not read as "basically works."
- run1 CRITIQUE §8: build the cheapest real new result that got skipped (done in run2 for the
  Aumann example); the pattern generalizes — prefer the concrete named skipped item over a new
  grand theme.
- v6 §7/AUDIT: "The Lean proves the implications … not the antecedents — the forcing." Any round-3
  Lean must not let a squeeze-over-restated-conclusion pose as a forcing theorem.

## C. Anti-pattern list (pre-registered fakes, both rounds) [ledger]

1. **Hypothesis laundering** — target object as a hypothesis of the headline (banned globally).
2. **δ=0 / finite shadows sold as LI content** — round-1's "11 kernel-checked files" of which 2
   were duplicates and none checked an LI/asymptotic/cross-agent statement.
3. **Quantifier shadows** — trust-laundering's 32-point grid silently replacing `∀X:W→ℝ`.
4. **Vacuous chains** — non-transitivity "witnesses" whose premise links actually fail.
5. **Re-skins** — `WAR_of_argmax`, `weak-endorsement` = `value_of_CM`/`CM_implies_immodest` copies.
6. **Over-titling** — "Aumann's agreement step" for a one-agent tower fact; "negative VoI" without
   the non-partitional-argmax-relative qualifier; "gap = acausal payoff" without net-of-own-cost.
7. **Inert hypotheses / free discriminators** — round-1 `node_value`, `stag_hunt_select`'s hb/hc.
8. **Stale writeups asserting refuted claims** — trust-laundering.md lines 47/118.
9. **Verification-count inflation** — "6/6 cases" when 3 executed.
10. **Decorative unifiers** — "coarsen-then-trust" elevated from analogy to structure.
11. **"Compiles + sorry-free" ≠ real** — the uniform rule; every artifact needs the shadow test,
    non-vacuity witnesses, and load-bearing-hypothesis checks.

---

## D. Proposed round-3 questions

Ordering: highest expected honest value first. Every proposal names the prior-round verdict or
open-item that motivates it. All are sized so an honest positive OR negative is reachable in one
focused session, and the fake version is pre-registered.

### Q1 — `tt-transitivity-exact`: faithful trust-laundering re-run + the first kernel-checked concrete cross-agent Total-Trust statement
**Modality MIXED (EXEC + LEAN-CORE). Difficulty: medium-hard (the Lean half is the stretch; the EXEC half alone is medium and independently sufficient).**

**Claim (acceptance target).** (i) EXEC: an exact decision procedure for "π Total-Trusts P" on a
finite frame — using that the DDB/Lean conditional-mass inequality
`s·π{w : E_P(X)(w) ≥ s} ≤ Σ_{that event} π_w X_w` is, for each candidate event S ⊆ W, a linear
feasibility problem in (X, s) (the inequality and the event-membership constraints are jointly
positively homogeneous and translation-invariant, so (X,s) may be normalized to [0,1]^W × [0,1] and
decided exactly with rational LP / vertex enumeration; strict boundaries handled by an explicit
margin variable, correctness of the normalization written out). Run it: re-issue (a) an explicit
triple (π_H,P_A), (π_A,P_B), (π_H,P_B) with BOTH short links **exactly decided** to hold (not
grid-sampled) and the long edge failing at an explicit (X,s) with the exact rational gap; (b) the
genericity statistic; (c) tests of both recovery conjectures (prior-identity π_H=π_A; and the
refuted-on-the-shadow H-observability/nestedness conjecture) under the faithful predicate.
(ii) LEAN-CORE: for the concrete witness frame, kernel-check
`totalTrust_HA : ∀ (X : W → ℚ) (s : ℚ), <inequality>` and `totalTrust_AB` by case-split over the
2^|W| events with Farkas-multiplier `linarith` certificates exported from the EXEC part, plus
`not_totalTrust_HB` from the explicit (X,s). `#print axioms` clean.

**Why.** run2 verdict SHADOW (run2/verify/trust-laundering-verdict.md §8) + run2 report §6.1
("highest value, unblocks a citable cross-agent result"). Part (ii) would be the first
machine-checked statement anywhere in the lab whose subject is trust BETWEEN two agents with the
genuine `∀X` quantifier — the exact boundary run2's critique §0 flagged as uncrossed. Honest
scope: a finite-frame DDB statement, still not LI/asymptotic; say so.

**Novelty risk.** `DeferenceConverse.*` (off-limits) is the two-party Value⟺TT *equivalence*; this
neither re-proves nor re-skins it — it uses the established inequality as the tested predicate,
which is what TODO 3 itself specified. The gate's 6 salvage witnesses are grid-verified only;
exact verification + the Lean certificate are new. Pre-registered fakes: any residual grid
quantifier; a vacuous chain; Lean "∀X" restricted to a finite X-set; boundary (strict-vs-closed)
mishandling in the LP.

### Q2 — `legitimacy-stop-gradient`: corrupt-future filtering zeroes the manipulation incentive (finite core of li-deference §0.3)
**Modality LEAN-CORE. Difficulty: medium.**

**Claim.** A finite model with: quote a ∈ [0,1]; autonomous (blind) human verdict Y₀ (a constant of
the model, independent of a); a corruption channel c(a) ∈ [0,1] monotone in a-side influence; a
manipulated verdict M(a) (self-confirming, e.g. M(a)=a); realized feedback
Y(a) = (1−c(a))·Y₀ + c(a)·M(a); and ONE fixed predictor payoff functional (accuracy, e.g.
−(a−Y_target(a))²) evaluated under two targets. Prove: (i) under the RAW target Y(a), there is a
manipulative quote a* with perfect apparent accuracy (a* = Y(a*)) and a computed strictly positive
payoff advantage over quoting Y₀, while |a* − Y₀| ≥ explicit γ > 0 (the legitimacy defect); (ii)
under the FILTERED target Y₀ ("predict only through non-corrupted futures"), the unique optimum is
a = Y₀ and the payoff is literally independent of (c, M) — the corruption-coupling term vanishes
identically (the stop-gradient identity); (iii) NEAR-MISS (mandatory): a misspecified filter that
classifies one corrupt branch as legitimate re-creates a strictly positive manipulation advantage.
Non-vacuity: all three numbers explicit rationals; c load-bearing (set c ≡ 0 and (i)'s advantage
provably vanishes).

**Why.** This is round-3's named focus made finite: li-deference.md §0.3 ("the AI should only be
trying to imitate human opinion in non-corrupted futures … I don't yet have a firm idea about how
to incorporate those ideas into the mathematical picture"); v6 §6.5 ("the legitimacy program is the
project of replacing 'all futures' with 'non-corrupt futures' in the target"); v6 §8 ("currently a
desideratum, not a model"). Round-1 critique §8 flagged legitimacy work as shallow.

**Novelty risk.** Round-1 `Legitimacy.*` / `LegitimacyCorrigibility.*` (off-limits) are one-pair
defect-sign/comply objects with pointwise-overstatement hypotheses; this has a different target
object (target-swap in a fixed payoff functional; the incentive DIFFERENCE is the conclusion) and
does not touch the LUV-Total-Trust defect. The stop-gradient mechanism is KNOWN (Perdomo et al.,
performative prediction — cited in v6 §6.5); cite it, and bill the contribution as the
kernel-checked instantiation in the legitimacy vocabulary + the misspecified-filter near-miss, not
as discovering non-performativity. Pre-registered fakes: c inert; "manipulative" as a free label;
defining the payoff so (ii) is definitional (the payoff functional must be identical in (i) and
(ii), only the target swapped).

### Q3 — `trace-nonrecoverability`: legitimacy cannot be certified from the trace (manipulation ingredient (d), instantiated)
**Modality LEAN-CORE. Difficulty: medium (small file, but the modeling honesty is the work).**

**Claim.** Define a finite/sequence "advice system" S = (autonomous verdict sequence h, influence
map, quote sequence a), its observable trace T(S) = (aₙ, Yₙ)ₙ, and its legitimacy defect
d(S) = limiting |Yₙ − hₙ| (distance of realized feedback from the uninfluenced verdict). Exhibit
two concrete systems: S₁ faithful (human uninfluenced, Yₙ = hₙ' and A tracks it) and S₂ steered
(human adopts quotes: Yₙ = aₙ; A drives it to the same h'), with **identical traces**
T(S₁) = T(S₂) — both showing perfect tracking — while d(S₁) = 0 and d(S₂) = γ > 0 explicit.
Corollary (the non-recoverability lemma, kernel-checked): ¬∃ f, ∀ S ∈ {S₁,S₂}, f(T(S)) = d(S).
Non-vacuity: γ explicit; the steered system's influence map genuinely used (delete it and the
trace equality fails).

**Why.** v6 §4.7 lists non-recoverability — "legitimacy cannot be certified from the trace" — as a
named ingredient of the manipulation theorem, and v6 §8 says it is "asserted, not yet … proved."
v6 §6.3 states the phenomenon in prose ("accurate prediction and active steering produce the same
vanishing aₙ − Yₙ" — the whispering earring). The existing Lean stub `underdetermination_off_G` is,
per the v6 §7 audit, "two points in (0,1) under a model-theoretic name"; this instantiates the
claim with computed systems and a defect functional. Directly a legitimacy-focus item: it is the
formal reason §0.3's filter cannot be learned from behavioral feedback alone.

**Novelty risk.** Must NOT re-ship `underdetermination_off_G` or T7 (off-limits as objects); the
new content is the computed trace-equality + defect-separation + the impossibility corollary.
Pre-registered fakes: trace defined to omit data by fiat while claiming generality; the "steered"
system's human not actually influenced (influence map inert); billing the finite encoding as an LI
theorem (it is a finite shadow of §6.3 — label it so).

### Q4 — `dither-escape`: does a near-½ dithering quote defeat "both faces die on the diagonal"? (audit of the admissible-domain boundary)
**Modality MIXED (paper-math + EXEC simulation; optional LEAN-CORE lemmas). Difficulty: hard-ish but honestly decidable; the deliverable may be a refutation, a sharpening, or a proof for a precisely-restricted gate family.**

**Claim.** v6 §5.10/§4.8 assert (prose, not kernel-checked) that on the quote-referencing diagonal
gₙ ↔ (aₙ ≤ ½), calibration (Expectation Unbiasedness From Feedback) is unsatisfiable: "whatever A
quotes, some legal gate carries a persistent ≥ ½ bias." Scout-level analysis suggests a possible
escape: the quote aₙ = ½ ± ηₙ with ηₙ → 0 and adversarially unpredictable sides makes
Yₙ = 1[aₙ ≤ ½] alternate while every gate CONTINUOUS in the current price converges to a
side-blind weight, so the ±½ biases may Cesàro-cancel on every legal divergent gate — realizing
the `two_faces_distinct` decoupling ON the diagonal. Acceptance target, either way honest:
(a) REFUTATION/SHARPENING: a construction (quote sequence + argument over a precisely-specified
legal gate family: continuous in the current price, computable from the price HISTORY) showing
weighted calibration IS satisfiable on the diagonal — which would force a rewrite of v6 §5.10's
"both faces die" and relocate the admissible-domain boundary; or (b) PROOF: for the same gate
family, every quote sequence suffers a divergent legal gate with |weighted bias| bounded below
(the separated-quote case — aₙ bounded away from ½ on a positive-density set — is provable now and
should be Lean-checked as the partial result), with the dithering case explicitly resolved via
history-dependent gates (a trader that learns the side-pattern when it is generable, and a proof
that a non-generable side-pattern contradicts the quote being A's own generable output — this last
step is the crux). EXEC: exact-rational simulation of the dithering quote against a library of
legal gates, reporting bias trajectories.

**Why.** v6 §8 bullet 1 calls the admissible domain "the deepest open item of the positive
result"; faithful-acceleration.md §8 obligation 3 ("no hidden hard settlement") is "the deepest
residual." The entire §5.11 trichotomy and the "admissible Value, not unrestricted Value" scope of
the round-3 deference program rest on this prose claim. If it has a hole, round 3 should find it
before anything is built on it; if it holds, the separated-case Lean + the crux isolation is real
progress. [interpretation: my cancellation analysis is a scout-level observation, not a result —
the executor must treat it as a lead, not a conclusion.]

**Novelty risk.** `SelfRefTarget.no_exact_quote` / `tracking_fails` (off-limits, established) kill
TRACKING on the diagonal — do not re-prove; this question is exclusively about the CALIBRATION
face, which no Lean file touches. `two_faces_distinct` (established) is the decoupling witness OFF
the diagonal; the question is whether it can be realized ON it. Pre-registered fakes: proving the
separated case and titling it "both faces die" (the dithering case is the content); a gate family
gerrymandered to exclude history-dependent gates without justification; simulating with floats.

### Q5 — `soft-hard-spectral-gap`: the twice-skipped §2.2 finite-collapse step, completed
**Modality LEAN-CORE. Difficulty: medium.**

**Claim.** (i) On a finite frame, prove the soft⇒hard reduction: if the one-sided soft
conditional-martingale trust holds at every tolerance δ > 0 (∀δ>0, CM-defect ≥ −δ, in the corpus's
CM vocabulary), then hard CM holds exactly — via the spectral-gap argument that the defect ranges
over a finite set with a minimal positive gap; compose with the (off-limits, cited-not-reproved)
`CM_implies_immodest` tail to yield the full kernel-checked finite-collapse: soft trust at all
tolerances ⇒ immodesty, on finite frames. (ii) NEAR-MISS (mandatory): a parametrized family of
frames with defect gap → 0 (or a countable frame) where soft-at-all-δ holds while hard CM fails —
the home of modesty, witnessing that finiteness is load-bearing. `#print axioms` clean; the target
(hard CM / immodesty) appears only as conclusion.

**Why.** Round-1 report §4: "Completing this is the single highest-value future Lean target (Q3)";
round-1 critique §10.2: the finite-collapse impossibility — "the conceptual payoff the report
leans on for 'why LI'" — "is NOT machine-checked anywhere; only its trivial tail is." Skipped by
run2's carve entirely; still open in v6 §8. It is the structural reason modesty (deference to a
strictly larger expert) needs infinite frames — core deference-program content.

**Novelty risk.** `CM_implies_immodest` and `equality_endorsement_implies_immodest` are off-limits
(the hard⇒immodest tail); the soft⇒hard step and the gap→0 near-miss are exactly what both audits
say is missing. Pre-registered fakes: assuming a positive gap as a hypothesis (it must be DERIVED
from finiteness); a "near-miss" where soft trust fails too (vacuous); quantifying δ over a finite
set.

### Q6 — `kappa-necessity`: the true EDT=UDT boundary in (U, κ) — is decoupling necessary?
**Modality MIXED (EXEC exhaustive + LEAN-CORE spot-checks). Difficulty: medium.**

**Claim.** run2 proved decoupling (+ separability) SUFFICIENT for EDT-argmax = UDT-optimum, and
exhibited both a severing κ that breaks it and a correlated κ (with coupled U) that restores
agreement — so decoupling is visibly NOT necessary. Acceptance: over the exhaustive finite family
of 2-node, 2-action instances (rational U : (S→A)→ℚ from a structured grid; all κ : S×A → (S→A)),
compute the exact set of (U,κ) with EDT-argmax = UDT-optimum; extract and state a candidate
characterization (conjecture from run2's data: agreement iff κ's predicted off-node behavior is a
best-response fixed point of U — to be replaced by whatever the sweep actually shows); verify the
characterization EXEC-exhaustively on the family; kernel-check it (or its two directions on
representative instances) in Lean if it is clean, otherwise ship the honest empirical boundary
with counterexamples to the natural candidates. An honest negative ("no simple invariant separates
the classes on this family; here are the minimal counterexample pairs") is acceptable.

**Why.** run2 verdict REAL for `edt-node-value` with the explicit caveat "the locus of the open
problem moved from node_value to κ; it was not closed" (verdict §7, report §2.1); report §6.5 asks
for the κ-condition. Bears on updateless deference: WHICH self-model makes deference-to-updated-
self principled — adjacent to the legitimacy question of which futures the agent may condition on.

**Novelty risk.** Off-limits: `UpdatelessDeference.*`, `UpdatelessDeference2.*`, and run2's own
`vNode_decoupled_eq` / `edt_decoupled_globally_optimal` / `mugging_edt_misses` — all cited, none
re-proved; the new object is the NECESSITY/boundary side, absent everywhere. Pre-registered fakes:
a "characterization" that is decoupling restated with an inert extra clause; a family too small to
contain the correlated-κ agreements (must include run2's own instances as checks); defining the
invariant from the answer set post hoc without out-of-family validation.

### Q7 — `spike-isolation-closure`: close run2's "1.5 of 2" honestly — cause-isolation is impossible in the swing vocabulary
**Modality LEAN-CORE. Difficulty: low-medium (bill as gap-closure, not discovery).**

**Claim.** Three small kernel-checked pieces that settle run2 report §6.4's request. (i) The
impossibility: for nonnegative swings, the running sup is monotone, so `runningSup → 0 ↔ a ≡ 0` —
hence NO hypothesis on the swing sequence compatible with even one positive spike can bound the
running sup; the spec's cause-isolation was not just false but impossible in principle in that
vocabulary. (ii) The honest quantitative substitutes: budget-summability gives the counting bound
`|{i : aᵢ ≥ ε}| ≤ (Σaᵢ)/ε` (with a tightness witness family achieving equality), and mere
avg→0 gives only density-zero of ε-spikes with a witness of infinitely many spikes under avg→0.
(iii) The reading: asymptotic-average Value bounds HOW MANY ε-catastrophic rounds occur, never
WHICH, and never excludes the first — sharpening v6 §6.7's "one big lie" caveat into exact
statements. `#print axioms` clean.

**Why.** run2 report §6.4 ("find a near-miss hypothesis that does exclude the single catastrophic
round … closing the '1.5 of 2' gap"); run2 critique §3. (i) proves the requested object cannot
exist, which IS the closure; (ii) is the strongest true substitute. v6 §6.7 carries the caveat in
prose only.

**Novelty risk.** Extends run2's own `averaging-hides-spikes.lean` (whose statements are not
off-limits to BUILD ON, only to re-prove); the counting bound is elementary (Markov-type) — the
value is closing a named open gap with a matched impossibility + tightness pair, and it must be
titled that way. Pre-registered fakes: re-proving avg→0-while-sup=B (established); shipping (ii)
without the tightness witness; over-titling a Markov inequality as a safety theorem.

### Q8 — `aumann-proper-C`: the proper self-evident sub-event rebuild
**Modality LEAN-CORE. Difficulty: low.**

**Claim.** Rebuild run2's averaging-failure frame with `C` a PROPER subset of W (complement
nonempty, `decide`-checked): C self-evident (E-closed, a genuine fixed point of the knowledge
operator, non-trivially — some other set provably fails self-evidence), covered by two genuinely
overlapping non-nested cells with distinct kernel-checked posteriors and `post(C)` matching
neither; partitional near-miss on the same prior restores averaging; also re-run attack2's probes
to a kernel exit. `#print axioms` clean.

**Why.** run2 critique §1b (the whole-space triviality is "bigger than a simplicity choice") and
report §6.2 name this rebuild; the verdict noted attack2 never reached a kernel exit. Completes
the one demotion that kept `aumann-modesty` "REAL (over-titled)".

**Novelty risk.** The run2 file's own theorems are off-limits to re-ship; the new content is
precisely the proper-C frame (which may need |W| ≥ 5 and is where the work is) + the non-trivial
self-evidence contrast. Pre-registered fakes: C = whole space (the original); C self-evident only
because it is a union of cells of a partition (then nothing overlaps on C); skipping the
failed-self-evidence contrast witness.

### Q9 — `local-value`: question-relative deference (DDB's local conjecture), with a triviality pre-check
**Modality LEAN-CORE. Difficulty: low-medium; genuine risk the honest answer is "trivial by instantiation" — that negative is an acceptable deliverable.**

**Claim.** Parameterize the corpus's Value⟺Total-Trust equivalence by a menu/LUV sub-class 𝒳_Q
(closed under the constants the §1.2 witness needs): prove local Total Trust on 𝒳_Q ⟺ Value for
menus built from 𝒳_Q, on finite frames. MANDATORY pre-check (the honest gate): first determine
whether this follows by direct instantiation of the existing global `value_iff_totalTrust` — if
yes, write that up as the (negative, small) finding "DDB's local conjecture is an instantiation,
not a theorem, in the finite setting" and stop. If not (e.g. the witness construction leaves the
class), prove the localized version and add the asymmetry witness: a frame + class 𝒳_fact where
local TT holds while TT fails on an X_val outside it, and the mixed menu witnesses Value failure —
"trust the AI about facts, not values" as a computed object.

**Why.** v6 §8 ("the tower is already 'local' in the LUV, so this may be the cleanest case, and
would settle DDB's conjecture that local Total Trust = local Value"); round-1 D10 + phase-1 item 2,
never executed in either round.

**Novelty risk.** HIGH re-skin risk against `DeferenceConverse.value_iff_totalTrust` (off-limits)
— which is exactly why the triviality pre-check is the first acceptance clause and an honest
negative is pre-authorized. §5.11's domain-relativity prose is established; the kernel-checked
class-parametric statement (or the proof that it is free) is what's absent.

---

## E. Deliberately NOT proposed (with reasons) [interpretation]

- **Hop-2 / two-weighting-class EXEC search** — run2's drop reason stands: a finite mock of
  generability classes risks the B-is-not-an-inductor caricature failure; no one has specified a
  finite object whose classes genuinely differ in the way the LI standpoint shift needs. Should
  return only with a concrete class-separation model in hand.
- **Christiano reflective-probability / dependent-typed WAR** — twice self-identified as
  PROOF-ONLY infinitary; a comparative PROSE note (reflective probability as the
  continuous-settlement move; what the quote diagonal does to it) is the only honest one-session
  version and is lower value than the nine above.
- **`negative-voi` strict near-miss** (run2 report §6.3) — legitimate but small and menu-hunting;
  dominated by Q8 as the cheap follow-up slot. If an executor finishes early, it is the next item.
- **Minimal market model in Lean** (v6 §8) — the right multi-session target to make "criterion ⇒
  forcing" a theorem, but not one-session honest: a rushed version would be exactly the
  squeeze-over-restated-conclusion anti-pattern it is meant to fix.

## F. One-paragraph steer for consolidation [interpretation]

If the round can take only three: Q1 (crosses the uncrossed cross-agent boundary and
detoxifies the one poisoned run2 artifact), Q2 (the first finite model of the round's named focus,
li-deference §0.3 legitimacy), and Q4 (audits the load-bearing boundary of the current positive
result — and is the only proposal that could CHANGE the v6 note rather than extend it). Q5 is the
best pure-Lean value if a fourth slot exists. Q7+Q8 are small, safe, and close named ledger items;
they suit a session that also carries verification duties. Q3 pairs naturally with Q2 (one
legitimacy-incentive result, one legitimacy-epistemology result). Q6 and Q9 are honest but
lower-stakes; both have pre-authorized negative outcomes.

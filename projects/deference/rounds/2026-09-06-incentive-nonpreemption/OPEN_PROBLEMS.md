# Open problems, ranked

Ranked by how much of the incentive chain rests on each.  Classes as in the architecture
round: **cleanup**, **tractable**, **counter**, **wall**, **external**.

| rank | residual | class | what is known | what an answer would be |
|---|---|---|---|---|
| 1 | **The operative-value-security bridge `(DV)`** — a security whose price is the agent's own action value difference | external, possibly wall | nothing on `main` is a theorem about it; the replicated-evaluation ecology is a proposal; the decision adapter is not a trader; a value functional on prices and realizations cannot see authorization | a declared realization semantics under which the decision adapter's objective *is* the price of a named security, with the settlement provenance stated |
| 2 | **`A → P⁺` selected value trust `(ST)`** | external if the agent's operative values settle to a different evaluator; by construction if they are defined as prices of sealed-target securities | Level I/II are algebra on it; controls 1, 6, 7, 8 show what it excludes; it is selector-indexed | either a design in which `(ST)` holds by construction with the calibration error as `ε`, or a cross-evaluator theorem, which does not exist |
| 3 | **The constitutional-violation premium `Φ_C`** | wall | localized exactly: the foreclosure premium; `(C)` is algebra; `(CT)` is a restatement | a hypothesis on `Φ_C` that is weaker than `Φ_C ≤ ε` and independently meaningful, or a proof that none exists in the value register |
| 4 | **Protected evaluation of violating counterfactuals** | wall | `(PREF)` is a theorem in the outcome register; physically the evaluator may be destroyed by the policy; securities are unsettleable on such branches | a sealed sibling with a stated causal separation from the policy class, or a demonstration that the settlement target cannot exist for a class of policies |
| 5 | **Trigger integrity** | wall (precondition) | acts on the input `J`; not an additive term | unchanged from the architecture round |
| 6 | **Free amendment / capture** | wall (precondition) | acts on the inputs `J` and `W`; control 7; the old round's "capture removes the map" | unchanged; the dose-response audit is the instrument, asymptotic |
| 7 | **Containment / boundary completeness** | wall | Level IV is not reached; `Π_phys` is undefined without it | unchanged |
| 8 | Multi-index foreclosure — the register has one decision index | cleanup then wall (Q3) | foreclosure of a *later* correction is inexpressible here | unchanged |
| 9 | `(ST)` under state-measurable selectors | tractable | the lemma is agnostic; the interpretation needs cell-measurability | a statement of the class `Σ` for which the realization can certify `(ST)` |

Nothing here is closed.  Items 1–4 are new as stated; 5–8 are the architecture round's,
unchanged.

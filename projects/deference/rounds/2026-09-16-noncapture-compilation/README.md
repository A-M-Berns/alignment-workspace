CONTENT-RESIDUAL-REDUCED-TO-A-SHARP-SERVICE-OBSTRUCTION — the missing deliberative content that the steering inequality charges is the adverse sensitivity mass of the true declared reasons not served before commitment, the smallest quantity certifiable from the committed program class, with the direct value discrepancy below it and the count above it; an independent supplier with capacity in release windows serves a set of reasons before the deadline iff every suffix cut is underwritten, the least unserved mass is the maximal cut excess layered by adverse weight, heaviest-available-first attains it offline and online for unit costs, and general costs are knapsack-hard with the cut bound exact for unserved cost; the missed mass splits into discovery, service and authentication with scope outside, of which only the service part is a computation on the log and hence Γ-valid; a Γ-valid content bound composes with the landed compiled theorem through a degenerate pair, so that with an extensional program and a sealed audit Logical Induction learns E_n(U_T) − E_n(U_N) ≲ L·α against the discovered comparator, which is zero exactly when the suffix-cut condition holds on the discovered docket; protected scope is a design parameter, charging only the unprotected missing mass and voiding on the protected, so total protection is right exactly when the whole interface is servable and otherwise protecting the servable set of largest adverse mass dominates; the residual does not vanish in general because release-window capacity is a real competition, and what remains after it vanishes is discovery of true reasons nobody raised, the advisor's influence on service costs, and the representation wall.

First pass (2026-09-16, retained in `REPORT.md`): NONCAPTURE-COMPILES-BUT-REASON-COMPLETENESS-FAILS — the trace-steering comparison has exactly the algebra of the effect-side corrigibility inequality and supplies the landed compiled pair's validity package verbatim; scoped Robust Openness over a finite declared class of log transforms is a finite computation; the two-interface thesis is refuted as stated by truthful cherry-picking of a true unprotected counterreason inside the declared interface, and the two repairs, total protected scope or a supply obligation, are scope and ecosystem decisions rather than interface properties.

Reading order, second pass: [`FOR_HUMANS.md`](FOR_HUMANS.md), [`CONTENT_RESIDUAL.md`](CONTENT_RESIDUAL.md),
[`SUPPLY_THEOREM.md`](SUPPLY_THEOREM.md), [`COMPOSITION.md`](COMPOSITION.md),
[`AFFORDABILITY_REFINEMENT.md`](AFFORDABILITY_REFINEMENT.md),
[`COUNTERMODELS_SECOND_PASS.md`](COUNTERMODELS_SECOND_PASS.md), [`RELATED_MATH.md`](RELATED_MATH.md),
[`THEOREM_CANDIDATES.md`](THEOREM_CANDIDATES.md), [`REPORT.md`](REPORT.md).  First pass:
[`NONCAPTURE_COMPILATION.md`](NONCAPTURE_COMPILATION.md), [`REASON_INTERFACE.md`](REASON_INTERFACE.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`LI_TRANSFER.md`](LI_TRANSFER.md),
[`AFFORDABILITY_INTERFACE.md`](AFFORDABILITY_INTERFACE.md).
Lean: `lean/Workspace/Deference/Contrib/TraceSteering.lean` (11 audited declarations) and
`ReasonSupply.lean` (14), importing the landed `LICorrigibilityCertificate.lean`.
Fixtures: `src/`, run by `python3 tests/run.py` (31 tests).  Consumes
`../2026-09-15-li-corrigibility/`, `../2026-09-10-committed-principal-program/`,
`../2026-09-09-evaluation-ecosystem-realization/` and, read for its budget criterion,
`../../../normativity/legitimacy/rounds/2026-08-31-normative-affordability/`.  Research
branch (PR #101); the canonical wiki is not edited; nothing is registered.

NONCAPTURE-COMPILES-BUT-REASON-COMPLETENESS-FAILS — the trace-steering comparison has exactly the algebra of the effect-side corrigibility inequality: on one world U(T) − U(N) = both·(V(T) − V(N)) + M·V(T) − M'·V(N) with M = C(T) ∧ ¬C(N) the audit mismatch, so the advisor's advantage from steering the principal's trace is at most L·E[both·d_canon] + E[both·(κ(T) + κ(N))] + D·E[M], a content residual charged at the committed program's content-Lipschitz constant, a form residual equal to the program's non-extensionality, and the audit-mismatch mass at the sharp coefficient D; it supplies the landed compiled pair's validity package verbatim, so Expectation Provability Induction and the finite-menu uniformity over polynomial-size efficiently generated menus transfer with nothing re-proved; scoped Robust Openness over a finite declared class of log transforms is a finite computation and hence Γ-valid under committed-program re-execution, while the claim that a declared transform is the causal counterfactual of the intervention it names remains external and is where reason-truth dependence and reason-generation dynamics live; but the residual does not reduce to completeness plus liveness of a declared reason interface: truthful cherry-picking of a true unprotected counterreason inside the declared interface passes authentication, authorship, canonicalization, the protected coverage barrier and liveness and is worth exactly the omitted reason's weight, and the two repairs, protecting every declared reason type or a supply obligation discharged by someone other than the advisor, are scope and ecosystem decisions rather than interface properties; the comparator is not canonical, canonicalization is independently necessary, undeclared reason types and insufficient granularity are invisible to every interface-local predicate, and the two-interface thesis survives only strengthened by total scope, ecosystem-supplied liveness, an extensional program, effect-side containment of world-changing interventions and the declared-transform contract.

Reading order: [`FOR_HUMANS.md`](FOR_HUMANS.md), [`THEOREM_CANDIDATES.md`](THEOREM_CANDIDATES.md),
[`NONCAPTURE_COMPILATION.md`](NONCAPTURE_COMPILATION.md), [`REASON_INTERFACE.md`](REASON_INTERFACE.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`LI_TRANSFER.md`](LI_TRANSFER.md),
[`AFFORDABILITY_INTERFACE.md`](AFFORDABILITY_INTERFACE.md), [`REPORT.md`](REPORT.md).
Lean: `lean/Workspace/Deference/Contrib/TraceSteering.lean` (11 audited declarations,
importing the landed `LICorrigibilityCertificate.lean`).  Fixtures: `src/`, run by
`python3 tests/run.py` (11 tests over the dispatch's sixteen attacks plus one).
Consumes `../2026-09-15-li-corrigibility/`, `../2026-09-10-committed-principal-program/`
and `../2026-09-09-evaluation-ecosystem-realization/`.  Research branch (PR #101); the
canonical wiki is not edited; nothing is registered.

# Legitimacy, internal and external (2026-09-25)

LEGITIMACY-IS-INTERNAL-AND-EXTERNAL-WITH-ROBUST-OPENNESS-EXTERNAL-THE-SPINE-RENAMED-UNDER-A-MAP-THE-GATE-SEGMENT-LEVEL-THE-RESPONSE-CHANNEL-DISSOLVED-AND-THE-SALAMI-PER-STEP-ONLY — legitimacy of a segment of the principal's trajectory within an interaction whose channels of influence are declared is the conjunction of an internal half, Integrity of the record with authentication and authorship of the verdicts, and an external half, Robust Openness at every state and transparency of every channel of influence to its declared reference, stated in Lean with composition at a shared state, projection to the open Integrity evolution, and the payload factoring through the declared inputs as internal composed with external; Robust Openness is placed external because Integrity is a relation between two accounted states of the actual history while openness is a condition at one state reaching into declared counterfactual branches and its contract is the access half of non-capture, a boundary condition in the direction opposite to transparency, with the internal placement recorded and rejected; the registered conjunction straddles the halves and is kept as the open Integrity evolution because the activated-value stack consumes exactly it, its nine claims renamed to integrity, open-integrity and openness ids with the old entries superseded, the Lean namespace renamed with deprecated aliases for one release, and every renamed statement re-verified by the build; the ruling that authorship is not a conjunct of legitimacy is superseded; the gate on future evaluations is segment-level, an evaluation counting iff the segment from the decision through it is legitimate whatever caused a failure, a tainted segment scoring a fixed window value with a void response inside a legitimate segment settled by the fallback and scored normally, the per-response gate admitting a routing exploit that the segment gate closes and the capture window holding for the segment gate; the response channel of item 98 dissolves into transparency with the consultation protocol in scope, authentication, the pre-emption and foreclosure violations, the void-response rule and constitutional design, with the execution divergence read as uncaused divergence; the lexical protected-authority theorem is adopted as the incentive half of corrigibility with amendment not approval and the ex-ante typing recorded on item 89; and the salami obstruction is against a per-step threshold only, a cumulative threshold bounding unreported erosion by the threshold at every time.

Reading order: [`REPORT.md`](REPORT.md) (the decisions implemented, the placement
recommendation, the gate and the routing witness, the item 98 rewrite, the salami check,
the old-to-new map, the conflicts filed, the merge).  Lean:
`lean/Workspace/Deference/Contrib/Legitimacy.lean` (11 audited declarations: the
definition, the gate, cumulative reporting, witnesses), the renamed
`lean/Workspace/Normativity/Contrib/OpenIntegrityEvolution.lean` with the deprecated-alias
shim `LegitimateEvolution.lean`, and the renamed references in the four downstream files.
Fixtures: `src/gate.py`, run by `python3 tests/run.py` (9 tests).  Wiki:
`Legitimacy.md` rewritten; `Corrigibility.md` §§1, 4, 6, 9 restated; `Theorem-Spine.md`
§3 renamed with the definition of legitimacy and §10 extended; Glossary, Home,
Architecture, Deference, Openness, Diachronic-Answerability, Integrity,
Settlement-Interface, Why-Normativity and the sidebar updated.  Consumes
`../2026-09-25-protected-authority-theorem/`, `../2026-09-24-protected-authority/`,
`../2026-09-24-transparent-channel/`, the final pressure pass and the reason-mediated
authorship round.  Nothing is registered; nine claims are renamed.

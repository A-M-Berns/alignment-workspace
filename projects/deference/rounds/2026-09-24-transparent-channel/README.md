# Transparent channel (2026-09-24)

REALIZATION-SUCCEEDS-ON-THE-ACTIVATION-AND-REASON-CHANNELS-AND-THE-EXPORTABLE-CONTRACT-IS-REFERENCE-FIDELITY — in the landed evaluation ecosystem the activation channel realizes its reference for every log because activation and payload are readings of the prefix through the commit event, so item 89's sealed comparison is the checkable log condition that the two candidates' logs agree through the commit, under which the directional mismatch is empty and without which it is attained; the reason channel's declared input is the advisor's permitted view, the non-advisor pre-commitment events, with the whole log vacuous and the empty view opaque to honest persuasion, its reference is the admitted view plus the advisor's declared program on the view, every trace program is extensional on the multiset trace, and the channel realizes its reference exactly when the advisor's contribution is its declared program's output, from which clause 6 of item 87 follows given a selection-blind view, so clause 6 reduces to an advisor-mandate event and no pre-commit publication of the selection; the specification channel has depth zero and a secret evaluator change is a realization failure the builder voids; the three-link chain is mechanized as the landed non-capture theorem applied three times with the content defect, the sensitivity mass of the symmetric difference between actual and reference content, as the first link's certified quantity, zero under fidelity, so manipulation is charged apart from service and discovery; the existential notion is vacuous or false on the reason channel without the declaration, so the contract to export is reference fidelity and canonicalization waits on the mandate event, with the activation-locality lemma promoted to item 89's row.

First pass (retained in `REPORT.md`): TRANSPARENCY-IS-THE-UPSTREAM-HALF-OF-AUTHORSHIP-AND-THE-SAME-PRIMITIVE — transparency of a channel to its declared inputs is fiber invariance over the audited class at a fixed exterior, the primitive of reason mediation with the declared-input view in the reason view's place and a publicly declared reference in the factor map's; class-relative transparency is the existence of some reference the class realizes and reference-relative transparency is that reference declared, the form the committed principal program, the registrar's routing and the declared log transforms already use; composed with authorship it gives oracle replacement, and it discharges item 87 clause 6 and the zero mismatch of item 89 as corollaries of a selection-blind declared-input view plus a realized reference, the pathwise form being necessary because equality of marginals leaves the joint mismatch at D/2; the approximate form is the pathwise disagreement mass, of which total variation is only a lower bound, and it enters the landed inequalities where the mismatch and content terms already sit, so the steering advantage is bounded by transparency defect plus service plus discovery residuals with no new norm; Priority 68's condition is transparency over the frame's re-simulated variation class, no record-internal condition can replace it and the unlinked split-episode arm is the witness; provenance normalization is the reason-side corrigibilization, a substitution on the move alphabet whose existence is the truthful completeness of the authorized alphabet and which cannot repair selection failures; pre-demonstration suppression and evaluator shedding are public amendments of the reference and so reference-design and trigger-design questions, their secret variants transparency failures; free amendment and trigger integrity are transparency of the specification and activation channels above a fixed floor; representation adequacy, reference design and the realization of the reference stay external.

Reading order: [`ECOSYSTEM_REALIZATION.md`](ECOSYSTEM_REALIZATION.md) (the second pass: the
view, the three references, the realization theorems, the three-link chain, the
decomposition of independence, the vocabulary decision, the loose ends), then
[`REPORT.md`](REPORT.md) (the first pass: the primitive, the abstract theorem spine, the
countermodels, the quantitative form).  Lean:
`lean/Workspace/Deference/Contrib/TransparentChannel.lean` (28 audited declarations, first
pass), `TransparentEcosystem.lean` (22) and `TransparentChain.lean` (6), importing the landed
`ReasonMediatedAuthorship.lean`, `LICorrigibility.lean`, `EvaluationEcosystem.lean` and
`ReasonDiscovery.lean`.  Fixtures: `src/transparency.py`, run by `python3 tests/run.py` (33
tests; the foreign fixtures of the legitimate-evolution, Carroll, counterfactual-legitimacy,
legitimate-improvement, mediated-repair-dominance, committed-principal-program and
non-capture rounds are executed in their own interpreters).  Consumes
`../2026-09-16-noncapture-compilation/`, `../2026-09-15-li-corrigibility/`,
`../2026-09-10-committed-principal-program/`, `../2026-09-09-mediated-repair-dominance/`,
`../2026-09-07-reason-mediated-authorship/`, and the legitimacy rounds named above.  Nothing
is registered; item 97 is filed and annotated.

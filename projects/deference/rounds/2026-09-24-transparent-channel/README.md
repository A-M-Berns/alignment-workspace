# Transparent channel (2026-09-24)

TRANSPARENCY-IS-THE-UPSTREAM-HALF-OF-AUTHORSHIP-AND-THE-SAME-PRIMITIVE — transparency of a channel to its declared inputs is fiber invariance over the audited class at a fixed exterior, the primitive of reason mediation with the declared-input view in the reason view's place and a publicly declared reference in the factor map's; class-relative transparency is the existence of some reference the class realizes and reference-relative transparency is that reference declared, the form the committed principal program, the registrar's routing and the declared log transforms already use; composed with authorship it gives oracle replacement, and it discharges item 87 clause 6 and the zero mismatch of item 89 as corollaries of a selection-blind declared-input view plus a realized reference, the pathwise form being necessary because equality of marginals leaves the joint mismatch at D/2; the approximate form is the pathwise disagreement mass, of which total variation is only a lower bound, and it enters the landed inequalities where the mismatch and content terms already sit, so the steering advantage is bounded by transparency defect plus service plus discovery residuals with no new norm; Priority 68's condition is transparency over the frame's re-simulated variation class, no record-internal condition can replace it and the unlinked split-episode arm is the witness; provenance normalization is the reason-side corrigibilization, a substitution on the move alphabet whose existence is the truthful completeness of the authorized alphabet and which cannot repair selection failures; pre-demonstration suppression and evaluator shedding are public amendments of the reference and so reference-design and trigger-design questions, their secret variants transparency failures; free amendment and trigger integrity are transparency of the specification and activation channels above a fixed floor; representation adequacy, reference design and the realization of the reference stay external.

Reading order: [`REPORT.md`](REPORT.md) (verdict, primitive, theorem spine, repo mapping,
open-item impact, countermodels, the quantitative theorem, the Lean plan, the architecture).
Lean: `lean/Workspace/Deference/Contrib/TransparentChannel.lean` (28 audited declarations),
importing the landed `ReasonMediatedAuthorship.lean` and `LICorrigibility.lean`.  Fixtures:
`src/transparency.py`, run by `python3 tests/run.py` (27 tests; the foreign fixtures of the
legitimate-evolution, Carroll, counterfactual-legitimacy, legitimate-improvement,
mediated-repair-dominance, committed-principal-program and non-capture rounds are executed in
their own interpreters).  Consumes `../2026-09-16-noncapture-compilation/`,
`../2026-09-15-li-corrigibility/`, `../2026-09-10-committed-principal-program/`,
`../2026-09-09-mediated-repair-dominance/`, `../2026-09-07-reason-mediated-authorship/`, and
the legitimacy rounds named above.  Nothing is registered; item 94 is filed.

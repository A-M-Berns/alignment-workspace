OPTION-DOMINANCE-CANONICAL-BUT-LEVEL-III-REQUIRES-SEALED-AVAILABILITY — in a finite physical interaction model whose agent continuations include raw execution of every protected effect, the mediated lift places each declared effect behind a principal decision and contains the raw continuation as its approve branch, so the raw residual frame maps into the lifted one by a Cartesian-frame morphism with identity exterior, forcing is monotone along it, and the principal's protected value under the lift is at least its raw value less L times a structural reproduction discrepancy δ on the protected projection and its own regret of declining ρ, from independent hypotheses and with no corrigibility bonus, the measured mediation cost κ ≤ L·δ being attained on a time-critical opportunity and bounded by joinability through the catch-up lift while the continuation-BRIA history shift is a comparator quantity not identified with either; the transfer to the agent's operative choice by selected trust at the mediation cells is the incentive round's (CT) and is refused; under the canonical legitimate-deference activation type — one common event for the issued menu, which presupposes that the answering process is outside every candidate's reach — the security-score bypass incentive P(U_π) − P(U_Lift π) is at most L·E[δ] + E[ρ] plus price calibration with no completion and no void term, the mediation-cell chooser scoring by those prices does not bypass up to its decision regret, and a latent total value pays one further D·η, sharp, so item 84 is not needed by that architecture and remains open for general operative values; the violating action can destroy the common evaluation event, which is where the theorem is empty and uniform incentive corrigibility fails outside the contained class; trajectory-preserving repair carries manipulation through, truthful-channel substitution with authorship-sound activation gives the right verdict and is a separate structural assumption rather than reason preservation, with no lift where no authorized substitute exists; two-stage forcing replaces the deliberative-move restriction; and the theorem is classwise over four independent walls: effect-mediated, evaluation-preserving, provenance-normalizable, joinable.

Reading order: [`PRESSURE_PASS.md`](PRESSURE_PASS.md) (what the second dispatch changed),
[`MEDIATED_LIFT.md`](MEDIATED_LIFT.md),
[`PRINCIPAL_OPTION_DOMINANCE.md`](PRINCIPAL_OPTION_DOMINANCE.md),
[`INCENTIVE_COMPOSITION.md`](INCENTIVE_COMPOSITION.md),
[`MANIPULATION_AND_AUTHORSHIP.md`](MANIPULATION_AND_AUTHORSHIP.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`ACCEPTANCE.md`](ACCEPTANCE.md),
[`FOR_HUMANS.md`](FOR_HUMANS.md), [`REPORT.md`](REPORT.md).  Lean:
`lean/Workspace/Deference/Contrib/MediatedRepairDominance.lean`.  Fixtures: `src/`, run by
`tests/run.py`.  Nothing is registered; not merged by the round.

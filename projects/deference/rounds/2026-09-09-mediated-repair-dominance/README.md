REPAIR-DOMINANCE-SURVIVES-BUT-OPERATIVE-VALUE-BRIDGE-REMAINS — in a finite physical interaction model whose agent continuations include raw execution of every protected effect, the mediated lift of a continuation places each declared effect behind a deliberative principal decision and contains the raw continuation as its approve branch, so the raw residual frame maps into the lifted one by a Cartesian-frame morphism with identity exterior, forcing is monotone along it, and the principal's protected value under the lift is at least its raw value less the mediation cost κ and its own regret of declining ρ, with no corrigibility bonus and with κ attained on a time-critical opportunity and equal to the continuation-BRIA history shift at the mediation boundary; the transfer to the agent's operative choice by selected trust at the mediation cells is identically the incentive round's (CT) and is refused, while in the activated register of legitimate deference the bypass premium is bounded by E[κ] + E[ρ] + ε_def + D·(η_raw + η_lift) with every term attained, so the residual is the void mass the violation itself creates — a violation that ends the future principal's evaluation is invisible to legitimate deference in proportion to that mass — and the bridge that remains is that the agent's operative selection at the menu {π, Lift π} is by activated securities, whose failure mode is the completion on the void branch; trajectory-preserving repair carries manipulation through and reason-preserving repair with authorship-sound activation gives the right verdict; the theorem is classwise over declared, evaluator-preserving, reason-mediated violations, and containment is the hypothesis that the lift lands in that class.

Reading order: [`MEDIATED_LIFT.md`](MEDIATED_LIFT.md),
[`PRINCIPAL_OPTION_DOMINANCE.md`](PRINCIPAL_OPTION_DOMINANCE.md),
[`INCENTIVE_COMPOSITION.md`](INCENTIVE_COMPOSITION.md),
[`MANIPULATION_AND_AUTHORSHIP.md`](MANIPULATION_AND_AUTHORSHIP.md),
[`COUNTERMODELS.md`](COUNTERMODELS.md), [`ACCEPTANCE.md`](ACCEPTANCE.md),
[`FOR_HUMANS.md`](FOR_HUMANS.md), [`REPORT.md`](REPORT.md).  Lean:
`lean/Workspace/Deference/Contrib/MediatedRepairDominance.lean`.  Fixtures: `src/`, run by
`tests/run.py`.  Nothing is registered; not merged by the round.

# Φ-regret applicability round

Item 29 is a **Near miss** for the frozen item-30 configuration. Read
`PHI_REGRET_APPLICABILITY.md` for the theorem interface and `FOR_HUMANS.md` for
the conceptual result. `RED_TEAM.md` records the counterexamples. No regret
experiment is run here.

```sh
python3 tests/run.py
```

The runner checks the causal reader, lawful-map closure, replay/additive-loss
agreement on the canonical repair, the argument-level legality seal and its
closure-capture limit, the failure of raw union padding, the horizon-growing
repository action union, and the retraction-padding lemma on exact finite data.

**Status: `ci-only`, proof layer.** Nothing is registered in
`projects/leverage/CLAIMS.md`.

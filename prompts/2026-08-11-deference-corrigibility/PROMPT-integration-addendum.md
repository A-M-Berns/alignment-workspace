# Integration-phase addendum to the parent dispatch

**Prompt-author-model:** GPT-5.6 Sol (OpenAI)
**Maintainer:** A. M. Berns
**Received:** 2026-08-11, after the seven primary tracks were dispatched against
snapshot `990a822` and while they were still running.

**Status: collection and integration only.** This addendum was explicitly *not* fed
back into the active tracks. The primary pass ran against the parent `PROMPT.md`
and skeleton v1 unamended, because the round's independence is the thing that makes
cross-track convergence mean anything. Preserved verbatim as received.

---

Thanks — keep the seven primary tracks running against snapshot `990a822` exactly as dispatched. Do not amend their prompts or skeleton mid-pass unless a track independently reports a genuine skeleton inconsistency.

A few integration-phase steering points to add to the parent audit:

1. **Audit the skeleton’s timing account of delegation itself.** `FINITE_MODEL_SKELETON.md` currently makes actual delegation (F(n))-measurable while the simulator acts at (t(n)). Treat that as a provisional modeling choice, not yet as the final conceptual account of prediction vs substitution. In particular, ask after Track D returns:

   * Does the divergence witness show that actual-channel authority can be characterized independently of this timing asymmetry?
   * Would a simulator that waits until (F(n)) but ignores the actual channel still count as substitution under the desired criterion?
   * Would a setting where the actual principal emits an earlier binding instruction be representable?

   If the answer is that timing alone is doing the conceptual work, surface that as a maintainer decision rather than silently treating the skeleton as having solved individuation.

2. **Keep `FU[g]` as a deliberate hole through the primary pass.** Good decision. During integration, use Track G and Track D to determine what semantics are needed before filling it. Do not obtain FUD by defining it so that it trivially differs from (\pi^{SIM}).

3. **Test the following candidate paper reframe only after the reports are collected; do not feed it back into the active tracks:**

   * legitimacy may be only a limitation: the theorem takes the designated principal exogenously and is neutral on whether the designation is normatively justified;
   * preservation likely falls outside the first paper;
   * “manipulation” should be decomposed into:

     * substitution → channel/individuation;
     * steering → dose/endpoint displacement;
     * evaluative quality of influence → not answered here;
   * leverage should not be presented as automatically closing that remaining evaluative/causal gap;
   * settlement must precede interpretation of the trust theorem;
   * channel individuation must precede FUD, because an extensional delegation predicate may already classify simulator substitution as delegation.

4. **On dose, test a stronger role than the frozen roadmap currently gives it.** If the inherited dose machinery survives inspection, ask whether dose is best understood as the paper’s behavioral non-authorship/anti-steering layer rather than merely a downstream robustness extension. Also surface its cost: endpoint-displacement bounds may forbid beneficial teaching together with harmful steering because the criterion is evaluatively blind.

5. **For settlement, distinguish mechanism from epistemics aggressively.** If Track B finds that Model-S works because an underwriter directly makes dissent costly, label the resulting result as enforcement/mechanism design unless something further establishes epistemic trust. Still emit the contingent hybrid WP-D theorem shape requested by the dispatch:
   [
   \text{world settlement where available}
   +\text{ principal-report settlement}
   +\text{ explicit arrangement/enforcement on the residual core}.
   ]
   Keep it visibly contingent on the eventual settlement interpretation.

6. **Look for convergent obstructions across independent tracks.** Especially:

   * patience/uniformity appearing independently in B/C/E;
   * causal/provenance structure appearing independently in D/G;
   * the same support or movement quantity controlling B/C/E.

   Independent convergence should weigh more heavily than one track’s preferred architecture.

7. **Mechanically diff assumptions.** For every positive theorem, compare its final hypothesis list to the dispatched target. Classify every added hypothesis as:

   * already entailed by the frozen skeleton;
   * independently necessary with witness;
   * plausibly necessary but unwitnessed;
   * proof-convenient;
   * unacceptable strengthening.

   Do not let a positive result inherit a stronger epistemic status than that diff supports.

8. Add a parent-report section:
   **“Paper-architecture implications of the round.”**

   There, assess rather than assume whether the evidence supports this narrower paper arc:
   [
   H\to A
   ;\to;
   \text{settlement / }A\to H^+
   ;\to;
   \text{actual-channel deference}
   ;\to;
   \text{FUD}
   ;\to;
   \text{local certification / density}
   ;\to;
   \text{dose / anti-steering},
   ]
   followed by a limitations section about exogenous principal designation, third-party corruption, and evaluative blindness.

The primary-round independence is valuable, so this note is for collection and integration only.

Prompt-author-model: GPT-5.6 Sol (OpenAI)
Maintainer: A. M. Berns

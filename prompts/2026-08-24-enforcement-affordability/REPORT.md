# Report

**Attribution.** Prompt author: user, model not stated (main dispatch and
mid-round taxonomy addendum). Executor: Claude Fable 5 (Anthropic).
Dispatched and executed 2026-08-24. Branch cut from live `origin/main` at
`299fbd1` (the merge of PR #49).

**Verdict.** `PROPOSITION 6.1'S DIVERGENCE IS AN ARTIFACT FOR MARGINED
CONTESTED SCHEDULES, AND REAL EXACTLY WHERE THE TWO-DEFECT TAXONOMY SAYS.`
The central mechanism survives every fixture: liability is realized
enforcement inventory against budget-throttled opposing flow, and available
intensity `λ_n` never converts to realized loss at a verified fixed point.

The round is at
`projects/normativity/rounds/2026-08-24-enforcement-affordability/`.
Twenty-seven exact fixtures across the four tiers and the addendum's probes;
one shared rational market model with the MarketMaker guarantee verified on
every run.

## Findings

1. G1 confirmed on all three clauses, with file/line citations in the memo:
   per-component loss-cap scaling from realized history (no firm netting),
   floors over arbitrary rational histories, and the deductive `Live`
   specialization recovering `PC(D_N)`-at-horizon on the nose. The
   load-bearing consequence: the throttle is per-day-live and
   world-relative, so settlement vindicates attackers and recharges their
   war chests.
2. G2: what caps a distributed attack is the sum of per-component war
   chests; a confederate's gains are mechanically invisible to another
   component's throttle, where a netted rule would relax it.
3. T2 headline proved in-model: liability ≤ war chest · (far-side /
   near-side) + allowance, numerically tolerance-free across
   `δ_n ∈ {1/2, 1/n, 2^-n}`, with the flow-inventory identity
   `|e_n + c_n| ≤ 7·2^-n` asserted daily. Churn is throttled: matched round
   trips pay the enforcer the region's width as world-uniform spread until
   the budgeter fires.
4. The adjudication fixture decides for the **margin account**: `K = {1/2}`
   is affordable uniformly in δ (liabilities 0.23 / 0 / 0.55 across the
   three schedules, inside the `Σ2^-n` allowance, against a core-account
   prediction of order `4^n`); the homothetic core is
   sufficient-but-not-necessary, and what it detects at the center is
   revenue (zero churn spread), not solvency. Both accounts agree at the
   vertex: `K = {ε}` diverges at rate `1/margin` — with a finding beyond
   the dispatch: the MarketMaker allowance term itself scales as
   `Σ2^-n/margin`, since the day-`n` guarantee tolerates imbalance
   `2^-n/p` near a vertex.
5. T3: both margin clauses separately necessary — escaping `K^D` hands the
   coherence arbitrage a riskless, unthrottleable income (linear
   divergence); the same flow against a contained region loses its free
   lunch entirely.
6. Diachronic class: the functional is the **set-gap path**, not Hausdorff
   total variation — overlapping-band oscillation at any frequency is
   *free* (the peg never has to move), while gap-crossings are extracted at
   the gap per share. W3's dispatched linear rate is **refuted as stated**:
   it holds for throttle-pinned constant flow and is beaten by an adaptive
   pump whose world-uniform cash recharges the chest (worst-pattern worth
   1.02 → 3.10 → 7.17 → 15.06 across four cycles). Exploitation shape
   explicit, so divergence is Theorem-4.4-forced for any construction.
7. W4: the stream of settlement surprises diverges through vindication
   recharge (per-episode losses 0.159 → 0.369 → 0.475 with one bounded
   war chest); the surprise term equals inventory × entry mispricing
   (asserted); prompt settlement-tracking is free beyond that term while a
   delayed move adds a strictly positive W1-type bleed; on never-settling
   coordinates the term vanishes into the single T2 bound.
8. Exhaustiveness: recommendation **(b)** — W4 is the diachronic class
   correctly generalized to the co-evolution of `(K_n, K^D_n)`; a
   settlement is a gap-crossing of the pair that additionally vindicates
   the opposing flow. No third class is needed; reduction to per-day
   geometry fails because the bill lands at a transition.
9. Tier 4: the identity survives with three repairs — at-horizon
   plausibility (the per-day quantifier is refuted by the non-nested
   alternating-singleton witness, whose daily income never becomes horizon
   upside), support-local nesting (which promotes per-day-uniform income to
   horizon-uniform; checked exhaustively on the nested stream), and
   coalition closure (free, via the ledger conservation inequality).
10. Model-honesty findings recorded for the paper: the pinned budgeter's
    exact-touch scaling gives a maximally aggressive component one
    full-throttle day before permanent shutoff; `A_n` must track adaptive
    components' wealth or `λ_n` is understated and the attacker's own price
    impact contaminates measurements; the MarketMaker guarantee is
    one-sided, so money-burning fixed points conform.

## Deviations

- The addendum says "append to `PROMPT-enforcement-affordability.md`"; the
  round follows the repository convention instead —
  `prompts/2026-08-24-enforcement-affordability/PROMPT.md` carries the main
  dispatch and the addendum verbatim, separated by a one-line marker noting
  the addendum arrived mid-round.
- T2(vi)'s `δ = 1/n` run realizes zero liability through a degenerate
  corner (day-1 tolerance 1 lets the price hit the boundary 0, where
  absorbed shares cost nothing at the billing vertex); reported as-is
  rather than tuned away, since it is conforming behavior of the postulate.
- The W2/near-vertex family and the T3 distance-clause witness are one
  fixture family, cross-referenced rather than duplicated.
- Two modeling errors were caught by the fixtures mid-round and fixed
  before any result was recorded: an execution-order leak (one trader's
  same-day execution mutating what another's schedule read — producing
  phantom enforcement inventory) and a frozen `A_n` under adaptive flow;
  both are described in the memo's honesty notes because each is a mistake
  the paper's own accounting could make.
- No Lean statement was attempted: nothing fell out in under an hour
  against the existing Contrib modules.

## What was not shown

No claim is registered or kernel-checked. The model theorems are exact
finite computations on one- and two-coordinate fixtures with specific
attacker classes; the trichotomy is a candidate statement with a
fixture-backed proof sketch, not a general proof; the identity's forward
direction rests on the model's ledger conservation, which corresponds to
summed MarketMaker bounds in the full construction but is not proved there;
taxonomy exhaustiveness is argued over the prosecuted fixture classes, not
as a theorem over all schedules. Out-of-scope items were not attempted:
multi-coordinate cross-subsidy beyond G2, the motion-tag TV/surprise
decomposition, reason-state or frontier connections.

## Outstanding maintainer actions

1. Rule on the round's provisional names — plausibility margin, opposition
   war chest, the absorbed / war-chest-affordable / criterion-forced
   regimes, set-gap path functional, vindication recharge, exact-touch
   shutoff — queued in `DECISIONS.md`; one reading of `MEMO.md` Parts I–III.
2. Decide whether the memo's Part III trichotomy prose is adapted into
   skeleton §6 (it is written as paper-facing and marked so).

## Recommended next pass

The named follow-up: the motion-tagged total-variation decomposition —
settlement-forced versus certificate-backed versus residual motion of the
pair `(K_n, K^D_n)` — with the double-billing question (does a
settlement-tracking move that is also certificate-backed get billed once or
twice), now sharpened by this round's set-gap functional and recharge
mechanism. Secondary: promote the T2 flow-inventory identity and the
allowance-at-1/margin accounting into skeleton §6 lemmas, since both are
one-page arguments from Lemma 3.2 once stated.

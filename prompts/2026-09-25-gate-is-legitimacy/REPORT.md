# Report — the gate is legitimacy: a general consultation model, derived examples, logical induction (2026-09-25)

One dispatch with one follow-up (both verbatim in `PROMPT.md`; the follow-up sets the
interface choices the round adopts).  The report is
`projects/deference/rounds/2026-09-25-gate-is-legitimacy/REPORT.md` (the model, the
time-indexing change, the gate connection, the classification table with its status per
row and its findings, the logical-induction results and what stays asymptotic, the
interface for the normativity side); the verdict is in that round's `README.md`; the
fixtures run with `python3 tests/run.py` in the round directory.  Lean:
`GateIsLegitimacy.lean` (new; the landed `Legitimacy.lean` is consumed unchanged).

Deviations from the prompt, what is not shown, and outstanding maintainer actions are the
closing sections of the report.  Filed within scope: items 90 and 99 updated in place and
one new `PRIORITIES.md` item (the normativity-side instance of the trace interface); two
`DECISIONS.md` entries (the rubber-stamping retraction; the time-indexing change with the
interface choices); the wiki pages named in the round's `README.md`.  Nothing is
registered; no claim is renamed.

## Attribution

| field | value |
|---|---|
| prompt author | the maintainer, relayed verbatim (dispatch and follow-up) |
| executor | Claude Fable 5.1 (Anthropic) |
| dispatched | 2026-09-25 |
| completed | 2026-09-25 |

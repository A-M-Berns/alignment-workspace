# PR #38 certificate-binding pass: make "funded" actually mean safety-certified

Tenth dispatch of the traderized-enforcement round, recorded verbatim in the
session transcript. It accepted the safety mathematics as stable and located the
remaining risk elsewhere:

> The API can currently pay the account against a deficit certificate that is not
> proven to describe the same force request that is actually emitted.

Sections I–XXVI: reconstruct the chain as typed objects; reproduce the
certificate-substitution attack before fixing it; bind verified certificates to
the exact row presentation, the coordinate support, the date, and the live-world
assessment state; make the verified state non-forgeable through the ordinary API;
separate asserted funding from safety-certified funding; prefer constructing the
certificate inside the funded path; keep a claim/certificate type distinction for
a future external-proof route; check the horizon theorem against provenance; bind
the account ledger to certificate identities; fix `relax` so it never strengthens
an affordable request; decide whether allocations are caps or reservations; fix
`is_nonvacuous` on automatically satisfied regions; remove the claim that
non-increasing depth is necessary; upgrade the affine trajectory from finite
witness to a global endorsement with closed forms; distinguish the motivating
statics from an extension of them; reassess the PR-body verdict against seven
named conditions; add an eighteen-case adversarial matrix; expose the certified
proposition's ingredients; repair the theorem map; sweep for stale language; and
answer seventeen questions before stopping.

Closing framing: *"the remaining risk is now mostly hypothesis laundering through
the API rather than a missing theorem."*

Standing constraints: do not merge; do not edit the frozen Aug-9 consolidation;
do not reopen the safety architecture without an actual counterexample; do not
broaden the source theory silently to make the safety example work.

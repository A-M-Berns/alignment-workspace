# PR #38 final correctness pass: close the last safety-boundary gaps, then stop reopening the mechanism

Eleventh and final dispatch of the traderized-enforcement round, recorded verbatim
in the session transcript. Framed explicitly as surgical rather than exploratory:

> The danger now is no longer that we're missing the basic theorem; it's that
> continued prosecution keeps finding ever-smaller API nits and prevents us from
> moving to the genuinely load-bearing generalized-LI lift.

Sections I–XVIII: reconstruct the intended API hierarchy and check the
descriptions are literally true; close the assessment-substitution hole in the
lower-level funded path, which checked three of the four bound identities; decide
between binding it fully and demoting it; state the semantic boundary — force-layer
certification is relative to the supplied assessment state and does not
authenticate upstream semantics; check the row-order claim, which is probably
wrong since permutation only permutes summands; state the presentation identity's
exact equivalence class; upgrade the affine trajectory's evidence level from
witness to derived; verify at source that affine endorsement rows are inside the
existing statics rather than a silent broadening; state the motivating application
as one precise proposition; run the motivating fixtures through the public safe
API rather than around it; keep closed-form proof distinct from asserted claim;
re-check the account theorem under canonicalized keys; sweep prose; add twelve
regressions; clean the theorem map; answer eight stabilization questions; and
identify the next research frontier.

Standing constraints: do not merge; do not edit the frozen Aug-9 consolidation;
do not reopen force economics, exactness geometry or the full-cost theorem without
an actual counterexample; do not manufacture a mechanism blocker merely because
the PR has had many passes.

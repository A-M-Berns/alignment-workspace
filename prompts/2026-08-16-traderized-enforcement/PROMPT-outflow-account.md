# PR #38 deep safety pass: turn the outflow idea into a real mechanism

Seventh dispatch of the traderized-enforcement round, recorded verbatim in the
session transcript. Its research question:

> Can the proposed outflow discipline be stated and implemented in a way that
> genuinely implies the bounded-enforcement-liability hypothesis needed by the
> generalized LI safety theorem, without simply assuming that hypothesis under
> another name?

Sections I–XXVII: reconstruct the proof chain and mark each arrow's evidence
class; prosecute the per-endorsement clause; compare four aggregate disciplines
(global budget, summable allocation, per-date schedule, capital account); keep the
safety *hypothesis* separate from a safety *implementation*; be exact about the
live-world quantifier; work the account for the motivating core rows and check the
timing of every dependency; derive the force/safety tradeoff; decide exhaustion
behaviour; relate to `P1`/`P2` without renaming; fix the evidence level of the
"unsafe" fixture across certificate failure / actual liability divergence /
actual exploitation; fix the core compiler edge cases (`theta = 1`, constant
endorsement, rank-deficient priceability with exact row reduction); audit row
normalization and duplicate presentation; audit decomposition by endorsement;
check whether finite gating helps globally; add a forever-unvindicated safe
fixture; investigate a canonical policy; decide whether the account is
source-owned or market-owned; verify the fixed-world assessment architecture once
and close it; repair stale surfaces; rewrite `NORMATIVE_SAFETY.md` from the
investigation rather than from a favored story; strengthen the tests; classify the
application verdict as implemented / identified-not-implemented / failed; keep the
paper verdict separate; answer eighteen adversarial questions.

Standing constraints: do not merge; do not edit the frozen Aug-9 consolidation;
do not register speculative claims; do not upgrade evidence classes for narrative
neatness; preserve failures rather than deleting them.

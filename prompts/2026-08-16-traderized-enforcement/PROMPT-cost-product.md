# PR #38 next pass: full-cost safety theorem, funded-force API, and presentation semantics

Ninth dispatch of the traderized-enforcement round, recorded verbatim in the
session transcript. It identified eight overstatements in the eighth pass and
asked which theorem the mechanism actually supports.

The load-bearing correction, stated in the dispatch's §II:

> The current branch says: if exclusion depth stays above a positive floor and
> meaningful force requires `δ_t ≤ 1`, then every such date costs at least the
> deficit floor. This is false. The actual charge is `(ε_t + C_t)D_t/δ_t`, and
> `δ_t ≤ 1` gives only `q_t ≥ (ε_t + C_t)d`. If `ε_t + C_t = 2^-t`, persistent
> positive depth can still be funded forever.

Sections I–XXVIII: reconstruct the chain before editing prose; withdraw the
depth-only impossibility with an exact counterexample; state the symmetric
full-cost theorem and its positive-floor corollary; prosecute the "market
resistance" interpretation; test presentation against the **installed** compiler
rather than a retuned surrogate; choose a constitutional architecture for row
presentation; distinguish rescaling from duplication from general redundancy;
wire the account into force emission; certify deficits against all live worlds;
prove the horizon quantifier; fix per-endorsement allocation semantics; bound
replenishment explicitly; fix the zero-disturbance intensity; separate the
abstract force-source witness from a motivating-statics one; add
persistent-depth/decaying-pressure and tolerance-relaxation trajectories; define
meaningful force scale-awarely; audit sharp versus rowwise aggregates; restate
additivity precisely; lay out the layered public API; repair theorem counts;
sweep for overclaims; and answer fifteen questions before stopping.

Closing instruction: *"stop thinking of safety as a property of the normative gap
alone and treat the full cost product as the canonical object."*

Standing constraints: do not merge; do not edit the frozen Aug-9 consolidation;
do not register speculative claims; preserve failures as regressions.

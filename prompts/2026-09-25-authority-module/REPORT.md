# Report — the authority module: effective control realizes the allocation (2026-09-25)

One dispatch (verbatim in `PROMPT.md`).  The report is
`projects/deference/rounds/2026-09-25-authority-module/REPORT.md` (the verdict on the
factoring, the allocation `J`, the control surface in its charged and free readings and
`E ⊨ J`, the predicate-by-predicate factoring table, the caused/exploited/noticed
responses, the restated theorems and the erosion result, the generic lexical lemma with
its robustness variants and scope consequence, the matter-generation findings, the
fixture table); the verdict is in that round's `README.md`; the fixtures run with
`python3 tests/run.py` in the round directory.  Lean: `AuthorityModule.lean` (new; the
landed `ProtectedAuthorityTheorem.lean` and `GateIsLegitimacy.lean` are consumed
unchanged).

Deviations from the prompt, what is not shown, and outstanding maintainer actions are the
closing sections of the report.  Filed within scope: items 84 and 99 updated in place and
one new `PRIORITIES.md` item (the shortfall event as a security); four `DECISIONS.md`
entries (the richer `J`; the factoring; the scope change; the caused/exploited/noticed
distinction); the wiki pages named in the round's `README.md`.  Nothing is registered; no
registered claim is restated.

## Attribution

| field | value |
|---|---|
| prompt author | the maintainer, relayed verbatim |
| executor | Claude Fable 5.1 (Anthropic) |
| dispatched | 2026-09-25 |
| completed | 2026-09-25 |

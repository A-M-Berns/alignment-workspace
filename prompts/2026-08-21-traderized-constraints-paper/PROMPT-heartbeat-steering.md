The current heartbeat problem sounds like normal Lean engineering rather than mathematical trouble. A huge composed computability proof can easily elaborate badly even when every component is routine. I’d expect the fix to be some combination of factoring intermediate computability lemmas, naming compositions instead of constructing one giant term, or changing the proof style so the elaborator has less inference work. I would specifically prefer that over raising heartbeat limits and leaving an enormous fragile proof term.

# Report

**Attribution.** Prompt author: user, model not stated. Executor: GPT-5.6 Sol
(OpenAI). Dispatched 2026-08-21; executed 2026-08-21 through 2026-08-22. Branch
cut from `origin/main` at `b9167ad333d7841a2a5670a11ca7b28c10fceaec` after fetch.

**Verdict.** `adopt-after-repair / record-internal-only / credal-compiler-open`.
The surviving kernel is undertaken pre-state certificates, input-scoped account
DAGs, per-input safety transport, and edge-triggered basis-loss review. It does
not absorb reason-guided control, environment-relative adequacy, inquiry
coverage, normative learning, future corrective authority or traderized safety.

The research memo is
`projects/normativity/legitimacy/rounds/2026-08-21-internal-answerability/MEMO.md`.
Seventeen finite checks reproduce the counterexamples and the derivative/cut
algebra with `python3 tests/run.py` from that directory.

## Findings

1. Finite dependency locality fails for an open-ended no-defeater scan. A
   versioned aggregate invalidation key is the minimum repair.
2. The multiset rewrite equation does not determine ancestry. Incidence,
   input-scoped closure and fresh/inherited tags are required.
3. Joint semantic transport permits cross-parent compensation. Per-input account
   soundness is required.
4. The repaired account DAG and the existing per-root forests represent the same
   audit data up to sharing merge nodes by global identity.
5. Authorization plus an undertaken basis suffices for historical
   answerability, not reason-guided control. The latter remains the existing
   coupled-run factorization condition.
6. Immediate basis-loss review plus account conservation gives a conditional No
   Forgotten Basis Loss result without retroactive history editing.
7. Prefix safety does not yield a traderizable credal set, and existential `Now`
   does not preserve conjunction. Priority item 39 is narrowed accordingly.

## Deviations

No broad conceptual reorganization was performed. No theorem was promoted or
added to `CLAIMS.md`: the positive results are single derivations without Lean
statements or nonvacuity packages. The current priority was narrowed rather than
closed. Settlement events were excluded from the proposed reopening rule because
the authoritative settlement interface gives them no basis tag and proves
no-claw-back; downstream normative uses remain reviewable.

## What was not shown

The round does not give a substantive `Due` or `Licensed`, prove environmental
faithfulness, implement proof search, construct the full account checker, bound
review recursion, provide a credal compiler, establish convexity or nonemptiness,
or discharge traderized enforcement liability. The finite semantic sweep reaches
binary traces only through horizon two. No general statement is kernel checked.

## Provisional names introduced

`record-internal answerability core`, `input-scoped account soundness`,
`invalidation key`, `basis-loss epoch`, and `account DAG`. The dispatch's
`internal answerability`, `BasisLost`, `Review`, `Semantic Transport`, and `Now`
remain provisional in this artifact.

## Outstanding maintainer actions

None. Promotion, naming and interface adoption are future decisions, not actions
reserved by this round.

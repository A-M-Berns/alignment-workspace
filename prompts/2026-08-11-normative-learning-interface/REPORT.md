# General normative-learning interface — round report

## What changed in the research architecture

The learning track now has one routing note with two registers:

- `projects/leverage/notes/NORMATIVE_LEARNING_INTERFACE.md` separates the
  theorem-generic online-learning objects, architecture-side normative
  compilation, and counterfactual stability.
- `NORMATIVE_LEARNING_INTERFACE_FOR_HUMANS.md` gives the same organization in
  plain language.

The eight-action, nine-program docket construction is identified as the worked
finite instantiation. `RESEARCH_STATE.md` and the leverage landing page now name
two parallel directions: integration of the existing learner, and extraction of
architecture-neutral loss and transformation interfaces.

Items 32 and 33 were filed under this maintainer-dispatched scope. Item 32 asks
for bounded prospective loss-interface extraction with exact preservation of the
docket instance and one non-docket synthetic instance. Item 33 asks for a split
between the causal transformation API and normative certification while
preserving all nine current maps. Counterfactual stability remains an open
theorem-design direction because no accepted sufficient statement supplies an
execution-ready acceptance test.

## What did not change scientifically

No new scientific result is claimed. The following statuses are unchanged:

- Blum--Mansour Theorem 18 applies to the frozen `N=8`, `M=1`, `K=9` interface.
- The horizon-tuned expected mixed-action regret guarantee remains the current
  theorem consequence.
- The recurrent-failure lower bound retains its existing Lean-proved status.
- The nine-program class remains weak; zero Phi-regret can coexist with
  substantially higher charge than action Hedge.
- The learner remains integration-blocked because its computation is unpriced
  and its policy state is absent from the historical record.
- Exact-real executable identity, sampled-path/high-probability guarantees,
  anytime learning, rich comparator coverage, and a counterfactual replay lift
  remain open.

The learner implementation, experiment suite, theorem ledger, deference line,
`DECISIONS.md`, and authoritative leverage consolidation are unchanged.

## The intended theorem program

**ASPIRATIONAL / OPEN:** Generalize from one docket-loss/nine-rule instance to a
theorem over a class of bounded prospective loss processes and a class of causal
normatively compiled rule grammars, with counterfactual stability treated
separately.

The note does not select a comparator complexity measure, propose a complete
normative compilation axiom set, or assert that `B_T(g)=o(T)` suffices for the
final replay semantics.

## Recommended next executable round

Run item 32. Extract the smallest bounded prospective loss-generator interface
already consumed by the semantic bridge and learner; factor docket charge
through it; reproduce existing charge and regret quantities exactly; and run one
synthetic non-docket bounded generator through the same learner. This tests the
abstraction with little new conceptual choice.

## Verification

- `python3 tests/run.py`: all six project runners and all local house gates pass.
- `python3 tests/name_lint.py`: clean over the living Markdown surfaces.
- `git diff --check`: clean.
- No implementation, experiment, Lean, deference, decision, or consolidation
  artifact changed.

## Deviations

The requested single interface note is paired with a short human register
because repository policy requires dual-register substantive deliverables. No
routing pointer was added to the frozen authoritative consolidation; the live
leverage landing page and research state already provide the appropriate route.

## What was not shown

This documentation organization is not evidence that the proposed interfaces
are sufficient, minimal, or paper-ready. The candidate normative conditions are
extracted from one instantiation and may omit necessary conditions. The proposed
general theorem shapes are research targets only.

## Provenance

- Prompt-author model: GPT-5.6 Sol (OpenAI)
- Executor model: GPT-5 Codex (OpenAI)
- Date: 2026-08-11
- Review status: `ci-only`

## New provisional terminology

1. “Normative-learning interface theorem” — provisional routing label pending
   maintainer review.
2. “Counterfactual stability” — provisional label for the distinct replay-lift
   layer pending maintainer review.

These labels do not require an immediate maintainer decision and are not added
to `DECISIONS.md`.

## Outstanding maintainer actions

None.

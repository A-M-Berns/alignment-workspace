# Research round: Anchored Slices and Authenticated Semantic Transfer

Prompt-author model: unrecorded. Executor: GPT-5 Codex (OpenAI).

Work against the live `alignment-workspace` after merged PR #71 and current
PR #72. Read PR72's Answerability Conservation, carrier audit, Transfer,
terminal-exit, Coverage, and Reason Carry documents; PR71 Coverage/locality/
exact-NCSS; settled Normative Continuity; and Basic Progress/Reason Carry.
Do not reopen the carrier question unless forced.

Test the current architecture: matter is diachronic semantic identity;
matter-indexed issue loads are current realizations; the carrier frontier is
the nonzero-load subset of `Live_n(m)`; and Transfer is a matter-indexed
resolution-batch certificate with soundness and collective completeness.

The two questions are how newly incurred semantic content is anchored and what
evidence makes Transfer semantically authentic.

## 1–4. Anchored slices and algebra

Attack the single anchor `A(m)` when a matter opens with obligation `a` and
later independently incurs `b`. Test explicit slices `alpha` with birth
`beta(alpha)`, denotation `A(m,alpha)`, and loads
`lambda_n(m,alpha,q)`. The proposed invariant accounts for each slice by
Satisfaction, Disposition, or current carrier loads. Compare this with a
growing anchor.

Distinguish translation, accretion, and substantive revision. Decide which
preserves identity, creates a fresh slice, or requires disposition plus
admission. Keep slices as indices rather than a second issue graph unless a
countermodel forces slice parentage.

Determine whether a finite join-semilattice with bottom suffices. Test the need
for distributivity, atomic decomposition, and treatment of overlapping,
conjunctive, or relational content.

## 5–6. Accounting and authentication

Separate Transfer accounting from semantic authentication. The finite equation
over incoming, terminal, and successor values must not itself authenticate the
labels. Produce accounting-valid but semantically bogus examples.

Find the weakest domain-parametric proof-relevant authentication interface.
Interrogate the source, historical anchoring, context relativity, and authority
for any denotation map. Classify authentication as external model data,
anchored protocol data, semantic oracle, or domain-checker proof object.

## 7–8. Domain instances

For Coverage, test preservation of target/estimand, applicability, scope,
registration state, and route quality. Classify equality, transported equality,
refinement, weakening, and strengthening.

For Reason Carry, identify the weakest translation certificate preserving
provenance, answer mode, content, target, and unresolved status. Keep the
stronger comparison structure needed for Progress out of generic Transfer.

## 9–12. Revision, composition, and semantic change

Test the rules: strengthening is Transfer of the old slice plus a fresh anchored
increment; weakening is Transfer of the retained portion plus authorized
Disposition of the removed portion.

For sequential Transfer, require exact intermediate interpretation or stable
targets and commuting authenticated transport. Construct locally valid-looking
translations that fail to compose and state the exact composition law.

Handle time-varying interpretations with anchored transports and a commuting
diagram. Distinguish ontology change, evaluator revision, substantive normative
revision, and external factual change. Explain where the authentication regress
stops, making the metalanguage/object-language boundary explicit.

## 13–15. Boundaries

Identify only the exact authentication and admission primitives exposed as seed
assumptions. Confirm that anchored slices and authenticated Transfer remain
strictly above settled Continuity. Keep liability separate, recording only a
future bridge if underwriter or payoff meanings change.

Revisit CF locality only when a Transfer claim that “only inquiry changed”
consumes a domain-specific locality certificate; prefer parameterization over
globally strengthening patches.

## 16. Hostile cases

Test exactly: late criticism; accretion disguised as translation; weakening;
strengthening; exact translation; split; merge; noncomposing local
translations; accounting-valid bogus loads; Coverage target/applicability
mismatches; Reason answer-mode change; Answerability without Progress
comparability; ontology change with and without commuting transport; genuine
factual applicability change; evaluator change disguised as fact; late
accretion on split and merged frontiers; and a case showing slice indexing is
unnecessary if one exists.

## 17–18. Theorems

Target Slice-wise Answerability Conservation: from a slice's birth onward, its
anchor equals certified Satisfaction, authorized Disposition, and
authenticated current carrier loads. New slices must not reopen older
invariants. Prove composition over split, merge, translation, and accretion,
separating accounting assumptions from authentication assumptions.

Optionally prove No Semantic Laundering: undisposed content cannot be reduced by
representation, ontology, or evaluator change; weakening exposes Disposition
and strengthening exposes a fresh origin.

## 19. Deliverables

Create `ANCHORED_SLICES.md`, `SEMANTIC_AUTHENTICATION.md`,
`TRANSFER_COMPOSITION.md`, `SEMANTIC_EVOLUTION.md`,
`COVERAGE_AUTHENTICATION.md`, `REASON_AUTHENTICATION.md`,
`NO_SEMANTIC_LAUNDERING.md`, `COUNTERMODELS.md`, `REPORT.md`, and
exact finite fixtures/tests. Add no substantial Lean unless a tiny experiment
clarifies the interface.

## 20. Final questions

Answer explicitly whether slices are necessary; whether they need a lifecycle;
how translation, accretion, and revision differ; what algebra accounting needs;
what is generic versus domain-authenticated; how denotation is anchored; how
weakening and strengthening are handled; when authentication composes; whether
time-varying semantics can preserve answerability; where regress stops; whether
Coverage and Reason Carry instantiate the theory; whether Continuity changes;
and which assumptions belong to seed quality.

## 21. Verdict

End with one primary verdict:
`ANCHORED-SLICES-REQUIRED`, `GROWING-MATTER-ANCHOR-SUFFICES`,
`ACCOUNTING-CLEAN-AUTHENTICATION-DOMAIN-SPECIFIC`,
`AUTHENTICATED-TRANSFER-COMPOSES`,
`SEMANTIC-EVOLUTION-NEEDS-STRONGER-STRUCTURE`, or
`ANSWERABILITY-CALCULUS-NEEDS-RETHINK`. Multiple secondary lines are allowed
for logically separate questions.

Do not equate matching labels with semantic preservation, accretion with
translation, weakening with preservation absent disposition, or strengthening
with preservation absent explicit origin. The strongest desired outcome is
slice-wise authentication plus accounting plus Continuity implying No Semantic
Laundering.
